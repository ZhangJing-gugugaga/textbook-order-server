<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.13</version>
    <relativePath/>
  </parent>

  <groupId>com.tian</groupId>
  <artifactId>textbook-order-server</artifactId>
  <version>0.1.0</version>
  <name>textbook-order-server</name>
  <description>教材征订系统服务端（MVP · Spring Boot 3 单体）</description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- 版本锁定（SPEC §1 / 03 §1） -->
    <mybatis-plus.version>3.5.7</mybatis-plus.version>
    <easyexcel.version>3.3.4</easyexcel.version>
    <jjwt.version>0.12.6</jjwt.version>
    <caffeine.version>3.1.8</caffeine.version>
    <springdoc.version>2.6.0</springdoc.version>
    <weixin-java.version>4.6.0</weixin-java.version>
    <archunit.version>1.3.2</archunit.version>
    <testcontainers.version>1.20.6</testcontainers.version>
    <!-- 本地真实 MySQL 集成测试（无需 Docker）。默认关闭；
         本机便携版 MySQL 的启动方式见 docs/deployment.md §5.1。
         启用：./mvnw test -Dmysql.local.enabled=true -->
    <mysql.local.enabled>false</mysql.local.enabled>
    <mysql.local.url>jdbc:mysql://127.0.0.1:3306/?useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=Asia/Shanghai&amp;useSSL=false&amp;allowPublicKeyRetrieval=true</mysql.local.url>
    <mysql.local.username>root</mysql.local.username>
    <mysql.local.password>root</mysql.local.password>
    <!-- 传递依赖版本收口（P3）：easyexcel 带入 POI 4.1.2 / commons-io 2.11.0，
         weixin-java-common 带入 bcprov-jdk15on 1.70 —— 均为已停止维护的旧版本。
         仅升版本，不改坐标，避免影响上层库的 API 兼容性。 -->
    <poi.version>5.2.5</poi.version>
    <commons-io.version>2.16.1</commons-io.version>
    <!-- POI 5.2.5 需要 commons-compress >= 1.25（ZipArchiveOutputStream#putArchiveEntry
         在 1.25 变更签名），easyexcel 带入的 1.24.0 会抛 NoSuchMethodError -->
    <commons-compress.version>1.27.1</commons-compress.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- easyexcel 3.3.4 声明 POI 4.1.2（2020 年版本、上游停止维护），此处收口到 5.2.5 -->
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi</artifactId>
        <version>${poi.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi-ooxml</artifactId>
        <version>${poi.version}</version>
      </dependency>
      <!-- commons-io 2.11.0 早于修复 CVE-2024-47554 的 2.14.0 -->
      <dependency>
        <groupId>commons-io</groupId>
        <artifactId>commons-io</artifactId>
        <version>${commons-io.version}</version>
      </dependency>
      <!-- POI 5.x 的 zip 读写依赖 commons-compress，需与 POI 版本匹配 -->
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-compress</artifactId>
        <version>${commons-compress.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-aop</artifactId>
    </dependency>

    <!-- ORM：MyBatis-Plus（含 DataPermissionInterceptor；SPEC §1 锁 3.5.x，
         3.5.8+ 重构分页/数据权限插件 API，与 SPEC §5 记载架构不符，故锁定 3.5.7） -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
      <version>${mybatis-plus.version}</version>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Excel：EasyExcel 流式读写（POI 版本经 dependencyManagement 收口到 5.2.5） -->
    <dependency>
      <groupId>com.alibaba</groupId>
      <artifactId>easyexcel</artifactId>
      <version>${easyexcel.version}</version>
      <exclusions>
        <!-- POI 5.x 用 poi-ooxml-lite 承载 schema；保留 4.x 的 poi-ooxml-schemas
             会在 classpath 上留下重复的 xmlbeans schema 类 -->
        <exclusion>
          <groupId>org.apache.poi</groupId>
          <artifactId>poi-ooxml-schemas</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- JWT：jjwt 0.12.x -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- 缓存：Caffeine（限频/只读缓存，不承载业务真源） -->
    <dependency>
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>caffeine</artifactId>
      <version>${caffeine.version}</version>
    </dependency>

    <!-- 契约：springdoc-openapi（OpenAPI 3 唯一契约源） -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- 微信小程序：code2session / 订阅消息 -->
    <dependency>
      <groupId>com.github.binarywang</groupId>
      <artifactId>weixin-java-miniapp</artifactId>
      <version>${weixin-java.version}</version>
      <exclusions>
        <!-- bouncycastle 1.70 三件套（bcprov/bcpkix/bcutil-jdk15on）已停止维护
             （jdk15on 线最后版本即 1.70）。已核实 weixin-java-* 4.6.0 的 1079 个 class
             对 org/bouncycastle **零字节码引用**（仅 POM 里残留声明），故排除无运行风险。
             bcprov 改用同一维护线的 jdk18on：类名/包名完全一致，仅坐标与所需 JDK 不同
             （本项目 Java 17），任何真实调用路径都能命中同一套类。 -->
        <exclusion>
          <groupId>org.bouncycastle</groupId>
          <artifactId>bcprov-jdk15on</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.bouncycastle</groupId>
          <artifactId>bcpkix-jdk15on</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.bouncycastle</groupId>
          <artifactId>bcutil-jdk15on</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>1.78.1</version>
    </dependency>

    <!-- 压缩包解析：导入上传的解压体积/条目数上限校验（POI 5.x 的 xlsx 用数据描述符
         流式写 zip，JDK 的 ZipInputStream 会抛 invalid entry size，故用 commons-compress） -->
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-compress</artifactId>
    </dependency>

    <!-- 可观测性：健康检查 / 指标（P3）。仅暴露 health，其余端点默认关闭，见 application.yml -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- ============ 测试 ============ -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- Testcontainers（Docker 可用时运行 MySQL 集成测试；不可用时自动跳过） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mysql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- -Djava.io.tmpdir 指向项目 target/tmp：测试 JVM 的临时文件（Mockito 引导 jar、
               Tomcat 临时目录、导入导出中间文件等，单轮约 13~26MB）默认写在
               C:\Users\<user>\AppData\Local\Temp，长期累积会挤占系统盘。
               落到 target/ 下既随 clean 一并清理，也不占用系统盘。 -->
          <argLine>-Duser.timezone=Asia/Shanghai -Djava.io.tmpdir=${project.build.directory}/tmp</argLine>
          <systemPropertyVariables>
            <!-- 本地真实 MySQL 集成测试（无需 Docker）：见 LocalMySqlIntegrationTest -->
            <mysql.local.enabled>${mysql.local.enabled}</mysql.local.enabled>
            <mysql.local.url>${mysql.local.url}</mysql.local.url>
            <mysql.local.username>${mysql.local.username}</mysql.local.username>
            <mysql.local.password>${mysql.local.password}</mysql.local.password>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
