---
version: 1
name: textbook-order-server
created: 2026-09-21
last-synced: 2026-09-23T03:30:00
last-modified-by: rpd-agent
status: completed
entry-type: continued
---

# 项目状态：textbook-order-server

## 一句话定义
教材征订系统的服务端：为 5 角色（教材室/秘书/教师/学生/供货商）× 小程序/Web 前端提供带 RBAC 数据隔离、两级审查、通知确认闭环与学期双缓冲切换的统一后端服务。

## PRD 摘要
- 核心功能（≤3 条）：1. RBAC 五表 + 学院级数据隔离（多角色并集）+ 供货商物理隔离只读接口；2. 征订窗口引擎（自动开关/无限次延长/提前截止+自动通知）与学期双缓冲原子切换；3. 教师填报两级审查、学生选购、通知确认闭环、Excel 异步导入导出
- 本版本不做：支付、微服务/消息队列、教学班模型、AI 功能、短信/企业微信催办渠道、前端页面实现
- 商业模式：校内免费
- 技术栈：Java 17 + Spring Boot 3.3.13 / MyBatis-Plus 3.5.7 / MySQL 8 / EasyExcel 3.3.4 / jjwt 0.12.6 / springdoc-openapi 2.6.0 / weixin-java-miniapp 4.6.0

## 功能进度清单
| 功能 | 子任务 | 优先级 | 状态 | 备注 |
|------|--------|--------|------|------|
| 认证与账号 | 登录/JWT(access+refresh)/锁定落库 | P0 | ✅ 已完成 | 401 三类语义（契约冻结） |
| 认证与账号 | 首登校验（手机后4位/openid）+ 强制改密拦截 | P0 | ✅ 已完成 | W19；业务接口 403 拦截 |
| 认证与账号 | 切换身份（不放宽数据范围）| P0 | ✅ 已完成 | W10 |
| 认证与账号 | 建号/停用踢下线/重置密码 | P0 | ✅ 已完成 | 初始密码=学号/工号后6位 |
| RBAC 与数据隔离 | 五表 + 37 权限码种子 | P0 | ✅ 已完成 | M1 冻结 |
| RBAC 与数据隔离 | @CollegeScope 多角色并集隔离 | P0 | ✅ 已完成 | 越权矩阵 30 例 100% 拒绝 + 审计 |
| RBAC 与数据隔离 | 供货商物理隔离 + ArchUnit 机检 | P0 | ✅ 已完成 | 包内禁 import 学生/教师 Mapper |
| 学期与窗口引擎 | window_status 落库真源 + 定时自动开关 | P0 | ✅ 已完成 | W11；仅扫 active；Asia/Shanghai |
| 学期与窗口引擎 | 手动开/提前截止/无限次延长 + 变更审计与自动通知 | P0 | ✅ 已完成 | version 乐观锁 + 进程内锁 |
| 学期与窗口引擎 | 窗口状态/serverTime/变更记录接口 | P0 | ✅ 已完成 | W24 |
| 双缓冲切换 | 学期域表 + user_semester_profile 归属真源 | P0 | ✅ 已完成 | W1/W6 |
| 双缓冲切换 | draft→active 单事务原子切换 + 归档只读 | P0 | ✅ 已完成 | 唯一约束兜底；冲突回滚 409 |
| 导入中心 | 异步批次（5 模板/进度/错误行不中断） | P0 | ✅ 已完成 | 万行实测 254s（含局部名单门禁扫描；≤5 分钟） |
| 导入中心 | 幂等 upsert + 停用比对限本次范围 | P0 | ✅ 已完成 | W14 |
| 教师征订 | 6 规则字段审查引擎（逐字段回显） | P0 | ✅ 已完成 | 契约冻结 {field,rule,message} |
| 教师征订 | 超管内容审核 + 关窗补正豁免 | P0 | ✅ 已完成 | W4；CORRECTION_EXPIRED 精确语义 |
| 学生选购 | 清单规则（本班任课 reviewed 并集/必修/下架） | P0 | ✅ 已完成 | W3 |
| 学生选购 | 覆盖提交 + 提交快照 + 参考用量汇总导出 | P0 | ✅ 已完成 | W15/W18 |
| 异动审批 | 逐条 + Excel 批量（batchNo）+ 立即生效 | P1 | ✅ 已完成 | W15/W16；通过写 profile |
| 通知确认闭环 | 任务合并/轮次重置 + confirm 幂等 | P1 | ✅ 已完成 | W7/W18 |
| 通知确认闭环 | 每日 09:30 订阅消息重发 + unauthorized 如实落库 | P1 | ✅ 已完成 | W5/W8；弹窗不设轮次上限 |
| 导出中心 | 同步流式/异步任务 + 一次性 token（410） | P0 | ✅ 已完成 | Q16；24h 清理 |
| 导出中心 | 教师明细/签字版/学生汇总/通知汇总/供货商 | P0 | ✅ 已完成 | 签字版模板驱动待样张替换 |
| 系统配置与审计 | 8 键白名单 + 值域校验 + 变更审计 | P1 | ✅ 已完成 | W8；不缓存立即生效 |
| 系统配置与审计 | 全操作留痕 + 多维查询 + 看板 | P1 | ✅ 已完成 | W24 |
| 供货商只读 | 按学院分组清单 + 一学院一 sheet 导出 | P0 | ✅ 已完成 | 字段白名单；导出写审计 |
| 联调交付 | API.md 手册（95 端点）+ OpenAPI 3 | P0 | ✅ 已完成 | 程序化交叉校验 100% 一致 |
| 测试与验收 | 单元/切片/集成/ArchUnit（241 用例） | P0 | ✅ 已完成 | 0 失败；性能用例默认禁用 |
| 试运行验收 | 订阅消息 5 轮重发 + P95<500ms 压测 | P1 | ⏳ 未开始 | 需 moonzj.com HTTPS + 已申请模板 |
| 前端页面（Web/小程序） | 全部页面 | P1 | ⏳ 未开始 | MVP 裁剪：前端主责，见各自仓库 |
| 支付 / 微服务 / 消息队列 / 教学班 / AI | 默认 | P2 | ⏳ 未开始 | MVP 明确不做（PRD 排除项） |

## 关键决策记录
| 日期 | 类型 | 决策 | 原因 | 影响范围 |
|------|------|------|------|----------|
| 09-21 | 技术选型 | MyBatis-Plus 锁 3.5.7（而非最新 3.5.x） | 3.5.8+ 移除 SPEC §5 记载的 DataPermission/Pagination API | 数据访问层全局 |
| 09-21 | 技术选型 | 集成测试用 H2（MySQL 模式）+ schema 转换器；Testcontainers 门控 | 本机无 Docker；生产 DDL 单一来源仍是 schema.sql | 测试层 |
| 09-21 | 架构模式 | 软删除显式实现（查询带 deleted=0，删除写毫秒时间戳） | W9 要求删除写时间戳，MP 逻辑删除只能写固定值 | 数据访问层全局 |
| 09-21 | 安全策略 | 审计分两种事务语义：关键动作同事务 + 登录/导出切面独立事务 | 审批/切换/复核需与业务同事务；无业务事务场景不阻塞 | 审计模块 |
| 09-21 | 安全策略 | 导出异步返回统一 {taskId, async, rowEstimate}；内部文件路径 @JsonIgnore | 联调前消除字段歧义与路径泄露 | 导出中心/供货商 |
| 09-21 | 业务逻辑 | 订阅消息未配置时如实记 unauthorized，弹窗为主触达 | W5/R10 机制上限（一次授权一条） | 通知模块 |
| 09-21 | 业务逻辑 | 导入初始密码批内并行哈希（BCrypt 纯 CPU） | 串行 10k 行 >10 分钟，并行后 169s 达标 | 导入中心 |
| 09-22 | 契约一致性 | 时间入参**两种格式都接受**（ISO 与 `yyyy-MM-dd HH:mm:ss`），由 `TimeFormats`/`TimeFormatConfig` 统一解析；不再依赖 `spring.mvc.format.date-time` | 该配置只管 MVC 参数绑定，body 由 Jackson 按 ISO 解析 → 联调实测「body 传空格格式 400、query 传 ISO 也 400」，且文案无法定位字段 | 全接口入参 |
| 09-22 | 业务逻辑 | 班级人数（W2）按名单**去重**重算 + 下调记 WARN 与审计 `classSizeShrinks` | 此前按行数计数：同学号重复行会放大上限；且局部名单静默收紧教师填报上限（联调实测班级人数被刷成 1） | 导入中心 / 教师征订 |
| 09-22 | 安全策略 | 供货商导出任务**双向物理隔离**：内部端点 `/api/export-task/{id}` 对 `bizType=supplier` 任务返回 404（非 ADMIN） | 此前只按 created_by 判定，供货商自己的任务经内部端点同样可读可下载，与「物理隔离」表述不符 | 导出中心 / 供货商 |

## 诊断记录
| 轮次 | 视角 | 问题 | 用户回答摘要 |
|------|------|------|-------------|
| R1 | 用户 | 目标用户与角色？ | 5 角色（教材室/秘书/教师/学生/供货商），Web + 小程序两前端 |
| R2 | 商业 | 商业模式？ | 校内免费 |
| R3 | 技术 | 技术栈与部署？ | Java 17 + Spring Boot 3 单体 + MySQL 8；moonzj.com 子路径 HTTPS 试运行 |
| R4 | 安全 | 敏感数据与合规？ | 名单不含身份证；初始密码=学号后6位 + 首登校验；JWT 环境变量注入 |

## 待人工确认（审查修复后遗留）
- [ ] **`notice.interval_hours` 目前不生效**：重发由每日 09:30 定时任务驱动，该配置只作任务快照展示（API.md §3.12 已如实标注）。要实现「按间隔跳过」需改 `resendTask` 判据（`last_sent_at + interval`），属订阅消息链路，建议与试运行验收一并处理
- [ ] **性能项（需 EXPLAIN / 真机实测后再定）**：① `notice_record` 缺 `user_id` 前导索引（`/api/notice/mine` 按 `task_id IN (...)` 扫，万人级单学期可达 15 万行，建议加 `KEY idx_notice_user (user_id, task_id, confirmed_at)` + 迁移脚本）；② `audit_log` 无 `at` 前导索引（只按时间范围筛审计 = 全表扫 + filesort）；③ 异动批量导入是同步单事务（每行约 9-10 条 SQL、无行数上限），万行级会长时间占用一条连接；④ 导入初始密码 BCrypt 对**全部行**预哈希（已存在用户也白算，重复导入白付 100% CPU）；⑤ 看板 `/api/admin/dashboard` 成本 O(全部用户)（3-4 万行、约 25 条 SQL）
- [ ] 批量审批「部分成功」语义：现为整批单事务 + 单次上限 500 条；若需逐条独立提交需改响应契约
- [ ] **班级人数（W2）语义**：已定「以名单为准」——学生名单导入把 `school_class.student_count` 重算为文件内该班**去重**学生数（下调记 WARN + 审计 `classSizeShrinks`，可用 `PUT /api/admin/class/{id}` 修正）；且**停用比对只在目标学期 = active 学期时执行**（draft 学期导入只写 profile，不动作账号状态）
- [ ] **学期 activate/archive 是否需要回滚**：现明确不可逆（归档后不能再次激活，无 active→draft 路径，接口文案已写明「归档不可逆」）；若要提供回滚，需先定「归档只读」保证与审计口径
- [ ] 数据库外键：全库关联列均为裸 BIGINT，引用完整性只在应用层（加外键影响逻辑删除策略，需 DBA 决策）
- [ ] Flyway/Liquibase 版本化迁移：schema.sql 已幂等但不做列级迁移，已上线库的结构变更需独立迁移脚本
- [ ] 窗口边界精度：window_end 之后最多 60 秒仍可提交（由每分钟 cron 决定），如需秒级精确需改为请求时实时判定
- [ ] `sys_user` 宽列拆分（reserve1~6 / field_check_result / submit_snapshot 在热表内）与全库 `SELECT *` 收敛
- [ ] weixin-java-common 的 xstream/dom4j 传递依赖未排除（httpclient 被 SDK 实际使用，无法排除；xstream 排除需实测微信链路）
- [ ] 提交/导出幂等键（前端配合，避免重复点击产生 409 噪音或重复任务）
- [ ] 多实例部署：分布式锁 / 外置缓存（当前限频、角色缓存、active 学期缓存、SEMESTER_LOCK 均为进程内）

## 当前阻塞项
- [ ] 订阅消息模板 id 申请（张敬，M1 首周）——未配置前重发记 unauthorized，不影响其他链路
- [ ] 签字版导出样张（田老师）——模板文件占位已就绪，替换文件即可，代码不改
- [ ] 万行导入/P95 性能量化验收——需 MySQL 生产等价环境
- [ ] 安全扫描 SEC-007 的 4 条误报（Java `Map.put/get` 变量名尾字母 `r` 命中 Express 路由正则）——非真实缺陷，真实鉴权由 Security 链 + @PreAuthorize 承担（越权矩阵已验证）

## 技术栈
- 语言/框架：Java 17 + Spring Boot 3.3.13（单体分层，按模块分包）
- 数据：MyBatis-Plus 3.5.7 + MySQL 8（utf8mb4）；refresh token/审计/配置落库
- 鉴权：Spring Security 过滤器链 + jjwt 0.12.6（access 15min + refresh 7d 轮换）
- Excel：EasyExcel 3.3.4（流式导入导出）
- 定时：Spring @Scheduled + 进程内锁（窗口扫描/通知重发/导出清理，Asia/Shanghai）
- 契约：springdoc-openapi 2.6.0（OpenAPI 3 为唯一契约源）
- 测试：JUnit 5 + AssertJ + Mockito + H2（MySQL 模式）+ ArchUnit；Testcontainers 门控
- 部署：Nginx 反代 + systemd + mysqldump 备份（docs/deployment.md）

## 变更日志
| 日期 | 变更内容 |
|------|----------|
| 09-21 | 初始化状态文件（Flow C：MVP 已完成，补录进度/决策/阻塞） |
| 09-21 | 安全扫描：修复 8 条 .gitignore 缺项；4 条 SEC-007 确证误报并记录 |
| 09-21 | 种子账号补齐：新增供货商 600003（首登待改密），五角色各 ≥3 且三态齐备（正常/首登待改密/停用），共 18 账号；README 账号表补全 |
| 09-21 | **架构与接口审查后全量修复**：5 项致命（JWT 弱密钥 fail-open、默认 profile 回落 local、供货商导出 IDOR、秘书跨院导出、异动审批写错学期/清空归属）+ 25 项严重（并发竞态/事务边界/越权/限频/健壮性）+ P2/P3 工程化项。测试 164 → 192 用例全绿 |
| 09-21 | 启动自检（fail-fast）落地：必填 profile、JWT 密钥强度（含已知弱值黑名单）、导出目录可写性；配置项新增 `TEXTBOOK_CORS_ORIGINS` / `TEXTBOOK_TRUSTED_PROXIES` / `TEXTBOOK_LOG_DIR` |
| 09-21 | 依赖收口：POI 4.1.2 → 5.2.5、commons-io 2.11 → 2.16.1、bcprov-jdk15on → bcprov-jdk18on、commons-compress 1.27.1；新增 actuator（仅 health）与优雅停机、traceId/MDC |
| 09-22 | **联调复核报告全量修复**：时间入参双格式统一（含 400 逐字段提示）、`reviewed` 终态文案（教师重提/管理员重复审核）、班级人数去重重算 + 下调留痕、供货商导出异步受理体补 `async`/`rowEstimate`、供货商任务双向物理隔离、学期归档不可逆文案与文档。测试 217 → 228 用例（0 失败 / 3 跳过），`scripts/e2e-smoke.sh` 45 项断言全通过（真实 HTTP + 本地 MySQL） |
| 09-22 | **上线前复审（五路并行审计 + 逐条复核后修复）**：① 窗口变更重置轮次漏 `deleted=0` → 第二次窗口变更撞 `uk_notice_round`，**窗口关不掉**（自动截止每分钟重试失败）；② 教师提交与审核并发会把 `reviewed` 静默改回（改行锁 + 重读）；③ 编辑学期信息回写整实体（丢 `window_status/version/active_status`，极端情况全站无 active 学期）；④ 首登 wxCode 分支可接管账号（改为只校验已绑定 openid）；⑤ 重置密码未清 `first_login_verified`；⑥ draft 学期名单导入会全局停用 active 在册账号；⑦ `TEXTBOOK_TRUSTED_PROXIES` 无占位符「配了不生效」（审计 IP 恒为 127.0.0.1）；⑧ 通知合并未并 `target_roles`（教师/秘书收不到窗口变更）；⑨ `local` profile 连非本机库拒绝启动、调度线程池 1→3。文档侧：部署手册补「建库建号/目录产物/备份凭据与演练」、替换仓库内 `<SECRET_...>` 占位、修 4 处过期数字、冒烟脚本改为**一次性库**运行（`scripts/smoke-isolated.sh`，连续 3 轮 45/45）。测试 228 → 241 用例（0 失败 / 3 跳过） |
| 09-23 | **生产测试报告（《后端测试报告-textbook-order-server-20260923》）失败项全量修复**：① **B11（P1）archive 无门禁**——`POST /admin/semester/{id}/archive` 此前不读 body，线上一次空 body 调用即把进行中的学期归档，全站业务停摆（只能整库备份恢复）；现要求 body 带 `version`（乐观锁，缺失 400），窗口进行中（`window_status=open` 或 `channel_open=1`）必须 `confirmWindowOpen=true`，否则 409 并说明影响；归档同时 `version+1`（否则归档前后 version 相同、乐观锁失效）。② **B15（P2）撤销归档**——新增 `POST /admin/semester/{id}/unarchive`（受限回滚：仅当前无 active 学期时可用，需 `version`+`confirm=true`，窗口保持 closed）。③ **B10（P2）重复 activate 语义**——body 改可选 + 校验顺序「先状态后参数」，空 body 重复激活由 400 变契约要求的 409。④ **B13（P1）局部名单防护**——学生名单导入按文件内人数重算班级人数（教师填报上限），下调比例 >20% 且 ≥5 人时导入 409（不建批次、不改人数）并回显逐班 diff，需 `confirmClassSizeShrink=true`；新增只读预览 `POST /admin/user/import/preview`（班级人数 diff / 将新建账号数 / 将停用账号数 / 错误样例），导入摘要补 `classSizeShrinkConfirmed`。⑤ **B12（P2）时间口径**——`PUT /admin/semester/{id}` 与窗口接口同口径（body 空格与 ISO 都接受，出参恒 ISO）；`LocalDate` 字段容忍带时间写法按日期取值；修正前端 `semester.ts`/`format.ts` 与交接文档里「body 只认 ISO、query 只认空格」的过期结论。⑥ **种子账号（P1 风险）**——新增 `db/cleanup-seed-accounts.sql`（幂等停用 18 个公开口令账号 + 撤销 refresh + 归属置不在册），部署手册补清理步骤。⑦ 前端配套：归档传 version+确认标记、archived 学期「撤销归档」入口、导入前预览确认弹窗。⑧ 修正 `LocalMySqlIntegrationTest` 的过期期望（`sys_role_permission` 50 → 43，收权后应为 28+6+4+3+2）。验证：H2 全量 248 用例 0 失败、MySQL 门控 9 用例 0 失败、`scripts/e2e-smoke.sh` 45/45、新增 `scripts/verify-report-2026-09-23.sh` 真库 60/60（逐条重放报告 §三 的复现步骤）、万行导入 254s（≤5 分钟）|
