<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tian.textbook.textbook.mapper.TextbookMapper">

    <select id="selectPageByFilter" resultType="com.tian.textbook.textbook.entity.Textbook">
        SELECT * FROM textbook
        WHERE deleted = 0
        <if test="isbn != null and isbn != ''"> AND isbn LIKE CONCAT(#{isbn}, '%')</if>
        <if test="title != null and title != ''"> AND title LIKE CONCAT('%', #{title}, '%')</if>
        <if test="author != null and author != ''"> AND author LIKE CONCAT('%', #{author}, '%')</if>
        <if test="press != null and press != ''"> AND press LIKE CONCAT('%', #{press}, '%')</if>
        <if test="status != null"> AND status = #{status}</if>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByFilter" resultType="long">
        SELECT COUNT(1) FROM textbook
        WHERE deleted = 0
        <if test="isbn != null and isbn != ''"> AND isbn LIKE CONCAT(#{isbn}, '%')</if>
        <if test="title != null and title != ''"> AND title LIKE CONCAT('%', #{title}, '%')</if>
        <if test="author != null and author != ''"> AND author LIKE CONCAT('%', #{author}, '%')</if>
        <if test="press != null and press != ''"> AND press LIKE CONCAT('%', #{press}, '%')</if>
        <if test="status != null"> AND status = #{status}</if>
    </select>
</mapper>
