# 教材征订系统 · 服务端 PRD（textbook-order-server）

## 概念版 PRD（≤ 200 字）

核心用户：教材室管理员、学院秘书、任课教师、学生、供货商（5 角色），及小程序/Web 两类前端。
要解决的一件事：为征订全流程提供带 RBAC 数据隔离、两级审查、通知确认闭环与学期数据双缓冲切换的统一后端服务。
产品形态：Spring Boot 3 单体服务（REST API + 定时任务 + Excel 导入导出）。
最小可用版功能（≤ 3 条）：
  1. RBAC 五表 + 学院级数据隔离（多角色并集）+ 供货商物理隔离只读接口；
  2. 征订窗口引擎（自动开关/无限次延长/提前截止+自动通知）与学期双缓冲原子切换（含按学期归属表）；
  3. 教师填报两级审查、学生选购、通知确认闭环、Excel 异步导入导出。
本版本不做：支付、微服务拆分、消息队列、教学班模型、AI 功能。
商业模式：校内免费。
技术前提：MySQL 8、moonzj.com（已备案 HTTPS）、账号超管分发制、小程序 openid 静默收集。
> 各模块的异常分支、状态机、字段校验规则与错误文案详见落地版；实现规格（DDL/接口清单/配置项）见本仓库 `SPEC.md`。

---

## 落地版 PRD

以下按概述 / 功能列表 / 服务结构 / 功能说明组织；每个模块均定义异常分支、状态机、字段校验规则与错误文案，全局规范见模块 8。

### 一、概述

**文档信息**

| 版本号 | 日期 | 作者 | 说明 |
|--------|------|------|------|
| V 1.0.0 | 2026-09-17 | ZhangJing-gugugaga | 首版（RPD 落地版，prd-validator PASS） |
| V 1.1.0 | 2026-09-21 | ZhangJing-gugugaga | **对齐三轮拍板结论**：复审 16 问（05 号）+ W1-W24 追问（06 号）全部采纳；见下方变更记录 |
| V 1.2.0 | 2026-09-23 | ZhangJing-gugugaga | **回写已上线实现**（按 `docs/13-后端文档对齐-goal-prompt-20260923.md`）：权限码 37→39、通知重发节奏、状态机死值清理、错误文案与配置键对齐；契约权威源明确为 `API.md` + OpenAPI。见下方 V1.2.0 变更记录 |

**V1.1.0 变更记录**

| # | 变更 | 依据 |
|---|------|------|
| 1 | 认证：新增首登校验（手机号后 4 位/openid 绑定后方可改密）、登录锁定落库、refresh token 轮换与撤销、密码复杂度 8-64 位含字母数字 | W19/W20/W21、Q6 |
| 2 | 学期与窗口：新增落库 `window_status`、`auto_open/auto_close` 边界、扫描仅 active 学期、时区固定 Asia/Shanghai、窗口状态/服务器时间/变更记录接口 | W11/W24 |
| 3 | 新增「学期数据双缓冲」机制：学期域表清单、按学期归属表 `user_semester_profile`、原子切换与归档 | W1/W6 |
| 4 | 导入：五张模板列清单、导入顺序、停用比对范围限定、导入为归属权威源 | W13/W14 |
| 5 | 教师征订：数量上限来源（班级人数，缺失回退配置）、关窗后被驳回表单的补正豁免 | W2/W4 |
| 6 | 学生选购：清单生成规则（教师审核通过教材并集）、校验规则、提交快照、汇总导出（参考用量） | W3/W18 |
| 7 | 异动：批量 change_request（批次号）、教师异动规则、审批通过对当前学期立即生效 | Q10/W15/W16 |
| 8 | 通知：确认主体改 `user_id`（覆盖秘书/教师/学生）、重发参数以 system_config 为唯一真源、触达机制结论（一次性订阅上限）、任务合并与弹窗队列上限 | W5/W7/W8/W18 |
| 9 | 供货商/导出：接口前缀统一 `/api/supplier/**`、供货商清单增数量列、秘书签字版导出、学生选购汇总导出、导出异步阈值与一次性下载 token | W12/W18、Q4/Q16 |
| 10 | 新增模块 9「系统配置与审计」：配置键清单 + 管理入口 + 审计查询 | W24 |
| 11 | 验证目标量化（万行导入 ≤5 分钟、P95<500ms、越权矩阵 100%） | W22 |
| 12 | 风险新增 R9-R13（账号劫持、订阅机制上限、导入误停用、双缓冲在途写、后端单线人力） | 06 §2/§11 |

**V1.2.0 变更记录（回写已上线实现）**

> 判定总则：系统已上线并冻结契约，**唯一契约源是 `API.md` + `GET /v3/api-docs`**。本版按「以代码实际行为为准」回写 PRD，不改变任何已上线接口的形状、状态码与错误码字符串。逐条依据见 `docs/13-后端文档对齐-goal-prompt-20260923.md`。

| # | 变更 | 依据 |
|---|------|------|
| 1 | 权限码 **37 → 39**：新增 `role:manage`（角色查看/新建/编辑/删除）与 `role:permission:assign`（角色-权限分配）；超管在鉴权层短路持有全部 39 条 | B-D1、BE-1/BE-2 |
| 2 | 首登拦截放行清单改为**显式枚举 4 个路径** + `/api/me*`，明确排除 `/api/auth/switch-role`（避免首登未完成的会话换发长效令牌） | B-D2 |
| 3 | 通知重发节奏：由「每日 1 轮」改为**每小时第 5 分钟扫描 + `notice.interval_hours` 间隔判定**（新任务立即发首轮）；`round_limit`/`interval_hours` 均为 `system_config` 实时真源 | B-D3、B-D4 |
| 4 | 补正截止定义：`correct_deadline = 窗口截止 + order.correct_window_days`；**删除「超管关闭补正」这一不存在的概念** | B-D5 |
| 5 | 教师征订单状态机清理死值：删 `field_check`（实际 5 态）；`reviewed` 明确为**终态**（系统无撤销审核路径） | B-D6 |
| 6 | 学生选购单状态机去掉 `closed`：仅 `draft`/`submitted`，关窗锁定由窗口引擎在提交时拦截 | B-D7 |
| 7 | 异动状态机：字段审查改为**提交时同步执行**，`pending_field_check` 标为历史死值 | B-D8 |
| 8 | 错误文案对齐：角色版本失效走 `REFRESH_INVALID`「登录已过期，请重新登录」（原「账号信息已变更」不存在于代码）；窗口并发冲突文案与实现逐字一致 | B-D9 |
| 9 | 配置键与端点口径：导入上限为 **DB `system_config.import.max_file_mb`**；异动批量导入为**异步批次（只返回 `{batchId}`）**；供货商导出任务为**两个端点**（查询 + `/download`）；导出任务查询为**登录 + 非 ADMIN 仅本人任务**（无权限码） | B-D10~B-D13 |
| 10 | `reserve1~6` 已补齐：`schema.sql` **26 张表全部带预留字段**（BE-6，迁移脚本已验零漂移） | B-D14 |


#### 1. 需求背景

- **目标用户现状**：教材征订依赖线下流程，无统一后端；前端（Web/小程序）需要一套支撑 5 角色隔离协作的服务端。
- **核心痛点**：无权限体系、无数据隔离、无通知确认追踪、学期更替数据切换要停服。
- **当前问题汇总**：

| 问题点 | 问题描述 | 当前影响 |
|--------|----------|----------|
| 无权限体系 | 5 角色权限靠人工约定 | 越权与泄露风险 |
| 无隔离 | 秘书/教师/学生/供货商数据范围无边界 | 隐私风险 |
| 无通知闭环 | 发送与确认无记录 | 漏订无法追责 |
| 学期切换 | 新学期数据更替需停服或人工换库 | 业务中断 |

> 上述痛点决定了各模块必须内建异常分支与数据校验；状态机与错误文案统一在「四、功能说明」与模块 8 汇总。

#### 2. 需求目标

- **业务目标**：
  1. 支撑 5 角色 × 万级学生规模的征订业务，数据隔离零越权；
  2. 学期数据双缓冲切换零停机、零感（在线用户无感，新学期导入不污染在用学期）；
  3. 通知发送/确认全链路落库可导出。
- **成功标准**：

| 指标 | 当前状态 | 目标状态 |
|------|----------|----------|
| 越权用例 | 无度量 | 越权矩阵 100% 拒绝 + 审计留痕 |
| 学期切换 | 停服人工 | 单事务原子切换，业务无感，同刻仅一个 active |
| 通知追踪 | 无 | 每轮发送时间/状态/确认时间完整落库（含教师/秘书） |
| 万行导入 | 前端解析易超时 | 异步批次，进度可查，**万行 ≤5 分钟** |
| 接口性能 | 无度量 | 列表接口 **P95 < 500ms**（1 万行数据量、单机） |

#### 3. 需求范围

- **包含**：认证与账号（含首登校验/会话轮换）、RBAC 与数据隔离（多角色并集）、组织与学期（含窗口引擎、双缓冲、按学期归属）、教材库/课程任课、教师征订两级审查、学生选购、异动两级审批（含批量）、通知确认闭环、Excel 导入中心、导出中心（含异步与一次性下载）、系统配置与审计、供货商只读接口。
- **不包含**：支付；微服务/消息队列；教学班模型；AI 功能；短信/企业微信催办渠道（V1.1 评估）；前端页面实现（见 textbook-order-web / 小程序 PRD）。
- 各模块的异常分支、状态机、字段校验规则与错误文案在「四、功能说明」逐项定义，通用规范见模块 8。

### 二、功能列表

| 模块 | 功能名称 | 子任务 | 功能说明 | 优先级 |
|------|----------|--------|----------|--------|
| 认证 | 登录/改密 | JWT 签发/强制改密/**首登校验** | 账号密码登录签发 access+refresh；初始密码（学号/工号后 6 位）首登强制改密，**改密前拒绝业务接口、校验手机号后 4 位（或绑定 openid）**；失败 5 次锁 15 分钟（落库） | P0 |
| 认证 | 会话管理 ▲ | refresh 轮换/撤销 | access 15 分钟、refresh 7 天落库；登出/停用/改密/角色变更撤销全部 refresh；401 三类语义 | P0 |
| 认证 | 账号管理 | 建号/停用/重置/导入建号 | 超管手动建号（含供货商）；Excel 全量导入即批量建号；停用不删除；openid 静默收集仅用于订阅消息 | P0 |
| 权限 | RBAC 与数据隔离 | 五表/CollegeScope | 权限码 `模块:业务:操作`（**39 条**清单随契约冻结：M1 冻结 37 + BE-2 新增 `role:manage`/`role:permission:assign`；见 `db/data-permission.sql`）；切面按角色注入过滤（ADMIN 全部/SECRETARY 本院/TEACHER·STUDENT 本人），**多角色取并集**；**切换身份不放宽数据范围** | P0 |
| 学期 | 学期与窗口引擎 | 自动开关/延长/提前截止 | 窗口起止任意设置；`window_status` 落库为唯一真源；定时任务到点自动开/关（`auto_*` 关闭时不动）；可无限次延长或提前截止后再延长；变更自动创建/合并通知任务并审计 | P0 |
| 学期 | 双缓冲切换 ▲ | draft→active 原子切换 | 学期域数据（课程任课/征订单/选购/通知/归属）按 `semester_id` 隔离；新学期导入只写 draft 区与 `user_semester_profile`，不污染 active；校验后单事务切换激活，旧学期归档只读；同刻仅一个 active（DB 唯一约束） | P0 |
| 数据 | 组织与名单 | 组织三表/名单导入 | 学院/专业/行政班三级（班级含人数，供数量上限）；学生/教师全量 Excel 异步导入（严格模式、幂等 upsert、**停用比对限本次导入覆盖范围**、按导入顺序执行） | P0 |
| 业务 | 教师征订+两级审查 | 字段审查/内容审核 | 一人一学期一单；提交先过系统字段审查（白名单客观规则）再转超管内容审核；驳回解锁该表单补正重提（**关窗后仍可补正，`order.correct_window_days` 天内**） | P0 |
| 业务 | 学生选购 ▲ | 清单/覆盖提交 | **清单 = 本班任课关系下教师已审核通过的教材并集**（必修标识）；窗口内一人一单，重提=覆盖；记录提交时学院/班级快照；汇总作「参考用量」导出给教材室 | P0 |
| 业务 | 异动两级审批 | 字段审查/内容审核/批量 | 教师/秘书提交学生或教师异动（逐条或 Excel 批量，共享批次号）；系统字段审查→超管内容审核→通过才落库生效（**对当前学期立即生效**，历史按快照） | P1 |
| 通知 | 通知确认闭环 | 任务/重发/确认 | 通知任务（手动 + 窗口变更自动，**同任务合并**）；未确认者按 `notice.interval_hours`（默认 24 小时）间隔重发订阅消息、最多 `notice.round_limit`（默认 5）轮（**参数以配置表为唯一真源，实时判定**；调度每小时第 5 分钟扫描，新任务立即发首轮）；确认首次生效落库；**弹窗通道不设轮次上限**；历史表按学期归档；汇总导出含教师/秘书 | P1 |
| 供货商 | 只读接口 | 清单/导出 | `/api/supplier/**` 独立模块物理隔离（包内禁 import 学生 Mapper，机检），字段 = 书名/ISBN/**数量**/教师姓名/学院；一学院一 sheet；导出留审计 | P0 |
| 导出 | 导出中心 ▲ | 明细/汇总/异步 | 教师征订明细（秘书本院/超管全院）、**秘书本院签字版**、**学生选购汇总**、通知汇总、错误明细；后端文件流 + 一次性 token；**>5000 行走异步任务** | P0 |
| 治理 | 系统配置 ▲ | 参数维护 | 重发轮次/间隔、数量上限回退值、补正窗口天数、导出阈值等键值维护，变更写审计 | P1 |
| 治理 | 审计日志 | 全操作留痕 + 查询 | 登录/导出/账号操作/窗口变更/学期切换/审批/复核/配置变更全记录；提供按操作者/动作/时间过滤的查询接口与页面 | P0 |

> 各模块的进入/退出状态机与错误文案规范在「四、功能说明」逐项定义；实现细节（DDL/接口/事务边界）见 `SPEC.md`。

### 三、服务结构与接口导航

```
[类型：单体分层服务，按模块分包]
├─ auth/         → 登录、JWT(access+refresh)、首登校验、密码策略、强制改密拦截
├─ system/       → RBAC 五表、账号、组织三表、审计切面与查询、系统配置
├─ semester/     → 学期、窗口引擎、双缓冲切换、按学期归属
├── textbook/    → 教材库、课程、任课关系
├── order/       → 教师征订单（两级审查）、学生选购
├── approval/    → 异动两级审批（逐条 + 批量）
├── importexport/→ Excel 导入中心（异步批次）、导出中心（同步/异步 + 一次性 token）
├── notify/      → 通知任务、订阅消息适配、确认追踪、重发调度
├── supplier/    → 供货商只读接口（物理隔离，禁 import 学生/教师 Mapper）
└── common/      → 统一响应、异常、审计注解、窗口校验注解、学期上下文
```

接口分组：`/api/auth/**`、`/api/me/**`、`/api/admin/**`、`/api/secretary/**`、`/api/teacher/**`、`/api/student/**`、**`/api/supplier/**`**；JWT 过滤器统一鉴权，数据隔离由切面自动改写 SQL。各模块的异常分支、状态机、字段校验与错误文案见第四章；供货商模块红线：包内禁止 import 学生相关 Mapper（**CI 机检：ArchUnit 扫描 import 列表，命中即失败**）。接口全量清单见 `SPEC.md` §11。

### 四、功能说明

以下每个模块均包含功能描述、流程、异常分支、状态机、字段校验规范与错误文案；全局异常码与文案令牌见模块 8。

#### 模块 1：认证与账号

**功能描述**：签发/校验 access+refresh；账号生命周期（建号/待改密/首登校验/停用/锁定/会话撤销）。

**用户流程（登录与首登）**
```
[POST /api/auth/login]
  ↓ 校验账号存在 + 状态正常 + 未锁定
  ├─ 密码校验通过 → 签发 access + refresh → 返回 must_change_password / first_login_verified
  └─ 失败 → 落库计数 +1 → 满 5 次写 lock_until(+15 分钟) → 统一错误文案（防账号枚举）

[首登分支（W19）] must_change_password=true
  → 改密前一律拒绝业务接口（仅 /api/me*、首登校验、改密可用）
  → 首登校验：手机号后 4 位匹配 或 openid 绑定成功
  → 校验通过后才允许设置新密码 → 状态=正常
```

**异常分支说明**：账号不存在/密码错误/已停用/已锁定返回同一文案「账号或密码不正确」（锁定态单独提示）；access 过期可 refresh 重放；refresh 失效/角色版本失效/账号停用 → 强制登出并区分提示。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 正常 | 建号/启用/首登完成 | 停用/锁定 | 可登录，全接口可用 |
| 待改密 | 初始密码首登成功 | 首登校验 + 改密成功 | 仅 `/api/me*`、首登校验、改密可用，业务接口一律 403 |
| 锁定 | 失败 5 次 | 15 分钟（落库 `lock_until` 到期） | 登录拒绝，重启不清锁 |
| 停用 | 超管停用/导入比对停用 | 超管启用 | 登录拒绝，token 与 refresh 即时失效 |

**字段规范**

| 字段名 | 类型 | 必填 | 限制 | 校验规则 | 错误文案 |
|--------|------|------|------|----------|----------|
| userNo | string | 是 | ≤32 | 学号/工号格式 | 账号或密码不正确 |
| password | string | 是 | 6-64 | BCrypt 比对 | 账号或密码不正确 |
| newPassword | string | 是 | 8-64 | ≥8 位且含字母和数字 | 新密码需 8 位以上且含字母和数字 |
| verifyCode | string | 首登时是 | 4 | 手机号后 4 位匹配（或 openid 绑定） | 校验信息不正确，请联系教材室 |
| refreshToken | string | 刷新时是 | — | 未撤销 + 未过期 + 用户状态正常 | 登录已过期，请重新登录 |

**错误文案**：登录失败「账号或密码不正确」；锁定「账号已锁定，请稍后再试」；停用「账号已停用，请联系教材室」；未授权/access 过期/refresh 失效/**角色版本失效**统一「登录已过期，请重新登录」（分别对应 `UNAUTHORIZED`/`TOKEN_EXPIRED`/`REFRESH_INVALID`，前端按 code 分流而非按文案）。

#### 模块 2：RBAC 与数据隔离

**功能描述**：权限码鉴权 + 数据范围自动过滤（多角色并集）+ 供货商物理隔离。

**状态机：** 无显式状态；隔离切面按角色注入过滤条件（ADMIN 不过滤 / SECRETARY `college_id` / TEACHER `teacher_id` / STUDENT `user_id`），**多角色取并集**；切换身份只改变界面与权限码呈现，不放宽数据范围。

**异常分支：** 越权访问资源（改 ID）→ 资源归属校验失败返回 403 并写审计；无权限码 → 403；供货商接口访问学生字段路径 → 404（路由不存在，物理隔离）。

**字段规范：** 权限码格式 `模块:业务:操作`（**39 条**，清单见 `db/data-permission.sql`：M1 冻结 37 + BE-2 新增 `role:manage`、`role:permission:assign`）；所有隔离查询列（college_id/user_id/teacher_id）必须建索引；供货商模块 CI 机检 import 红线。

**错误文案：** 「无权访问该页面」/「无权执行该操作」。

#### 模块 3：学期与窗口引擎

**功能描述**：学期生命周期 + 征订窗口自动/手动控制 + 变更自动通知 + 按学期归属。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 学期 draft | 建学期 | 双缓冲切换 | 数据可导入不生效（不参与业务查询与窗口扫描） |
| 学期 active | 切换（原子，DB 唯一约束保证同刻仅一个） | 归档 | 承载当前业务 |
| 学期 archived | 新学期激活 | — | 只读可查可导出 |
| 窗口 not_open | 学期 active 且未到 start | 到点自动（`auto_open=true`）/手动开 | 填报/选购校验拒绝 |
| 窗口 open | `window_status=open` 且通道开 | 到点自动（`auto_close=true`）/提前截止 | `@WithinWindow` 校验通过 |
| 窗口 closed | 超 end/提前截止 | 延长（无限次） | 校验拒绝 409；**被驳回表单补正除外** |

**异常分支：** 定时任务幂等（状态已变跳过）且**仅扫 active 学期**；`auto_open/auto_close=false` 时到点不改状态；切换与窗口变更加进程内锁 + 乐观校验（`version`）防竞态；延长早于当前时间被校验拦截；每次变更写审计并自动创建/合并系统通知任务（含新截止时间，范围=秘书+教师+学生）；**时区固定 Asia/Shanghai**（服务器 UTC 不影响判定）。

**字段规范：** `window_start < window_end`；通道开关独立于时间；切换请求携带预期版本号（乐观锁）；`window_status` 为落库唯一真源。

**接口（W24）**：`GET /api/semester/window/status`（含 `serverTime`，前端倒计时不信任本地时钟）；`GET /api/admin/semester/{id}/window/changes`（变更记录：谁/何时/原值→新值）。

**错误文案：** 「本期征订已截止」（409）、「征订尚未开始」、「存在更新的窗口配置，请刷新后重试」。

#### 模块 4：双缓冲切换与导入中心

**功能描述**：draft 学期全量导入→校验→原子切换；Excel 异步批次解析；按学期归属写入。

**用户流程**
```
[上传 .xlsx] → 落盘 + 批次落库 → @Async(专用线程池) EasyExcel 流式解析 → progress_pct 轮询
  ├─ 行级校验失败 → 错误行收集（不中断）→ 完成后错误明细可下载
  └─ 全部通过 → 数据入 draft 区（学期域表 + user_semester_profile）→ 人工确认
       → 单事务切换 active（旧学期 archived）+ 同步 sys_user 归属冗余列
```
**导入顺序（外键依赖，W13）**：学院 → 专业 → 班级 → 教师 → 课程任课 → 学生 → 异动。

**异常分支：** 文件魔数/类型/大小前置校验；解析中断批次标记 failed 可重传；幂等 upsert（user_no/ISBN 存在则更新）；**导入为归属权威源，与异动冲突时以导入为准并提示「覆盖 N 条异动结果」**；导入期间 active 学期查询零影响。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 批次解析中 | 上传成功 | 完成/失败 | progress_pct 递增 |
| 批次完成 | 解析完成 | — | ok/error 计数 |
| 批次失败 | 解析异常 | 重传 | 失败原因 |
| 学期 draft | 建学期 | 切换 | 数据不生效 |
| 学期 active | 原子切换 | 归档 | 承载业务 |

**字段规范：** 上传仅 .xlsx ≤10MB（`system_config.import.max_file_mb`）；行级校验（必填/格式/外键存在）；严格模式不自动创建学院/班级；**停用比对仅限本次导入文件覆盖范围**（按文件内学院 + 角色限定，W14）；所有表含公共字段 + `reserve1~6` 预留字段。

**错误文案：** 「仅支持 .xlsx 文件」「第 N 行：{校验错误}」「切换失败：存在更新的学期状态，请刷新」。

#### 模块 5：教师征订两级审查与学生选购

**功能描述**：征订核心业务；审查链=系统字段审查（自动白名单）→超管内容审核（理由必填）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| draft | 创建 / `pending_review` 撤回（BE-4） | 提交 | 可改 |
| rejected_auto | 字段审查失败 | 修复重提（窗口内不限次） | 错误逐字段返回 |
| pending_review | 字段审查通过 | 超管审结 / 本人撤回 | — |
| reviewed | 超管通过 | — | **终态**；计入汇总；**进入学生清单** |
| rejected | 超管驳回 | 补正重提（仅该表单解锁，**关窗后 `order.correct_window_days` 天内仍可补正**） | 理由落库；学生清单中该教材标「已下架」 |

> **实际共 5 态**（`draft`/`rejected_auto`/`pending_review`/`reviewed`/`rejected`）。`field_check` 与 `submitted` 在代码中**从不写入**（`order_form.status` 注释已标为历史死值，BE-8），状态机以本节为准。

学生选购：draft → submitted（覆盖语义，重提=整单替换），**只有这两态**；**关窗锁定由窗口引擎在提交时拦截**（409 `WINDOW_CLOSED`），不是单据状态。记录提交时 college/class 快照，异动不影响历史归属。

**异常分支：** 关窗瞬间提交 409（**补正重提豁免**）；审查规则全为教师可控可修复的客观项（禁止"全驳回"死局）；并发提交以唯一约束 + 覆盖语义裁决；复核与补正并发以后端状态机裁决。

**字段规范：** 明细行=课程×班级×教材×数量（1 ≤ 数量 ≤ **班级人数**，班级人数缺失回退 `system_config.order.quantity.max_default`，W2）；教材必须在库；课程归属教师本人；驳回理由 1-200 字必填；**学生选购清单来源=本班任课关系下教师已审核通过（reviewed）的教材并集**（W3）；学生数量 1-9 且受班级人数约束。

**错误文案：** 「存在 N 项问题，请按提示修复后重新提交」「本期征订已截止，提交未生效」「存在更新的表单状态，请刷新后重试」「部分教材已下架，请核对后重新提交」。

#### 模块 6：异动两级审批

**功能描述**：学生/教师异动申请的审批流（逐条或 Excel 批量），通过才落库。

**状态机：** 字段审查在**提交时同步完成**——通过 → `pending_review`；不通过 → 直接落 `rejected` + 回显 `fieldCheckResult`。审批端 `pending_review` → `approved`（落库生效：写 `user_semester_profile` 的 active 学期归属）/ `rejected`（理由必填）。**`pending_field_check` 为历史死值**（2026-09-22 起不再产生，BE-7d）。

**异常分支：** 字段审查（目标学号/工号存在、目标学院存在、学生需目标班级存在、类型合法、前后值不同）失败自动驳回并逐项提示；审批通过落库与审计同事务；批量模式逐行审查、支持按批次批量通过/驳回（理由必填）。

**字段规范：** `type ∈ {student, teacher}`；`payload_json` 记录变更前后值；**教师异动只校验 工号存在/目标学院存在/前后值不同，可变更字段=`college_id`**（W16）；批量批次共享 `batch_no`；理由 1-200 字必填（驳回时）。

**生效时点（W15）：** 通过后对 **active 学期立即生效**（历史表单/选购按提交快照不变）；导出同时给出「快照归属」列便于对账。

**错误文案：** 「目标学号不存在」「目标班级不存在」「变更内容无变化」。

#### 模块 7：通知确认闭环

**功能描述**：通知任务（手动 + 窗口变更自动，同任务合并）；未确认者按 `notice.interval_hours` 间隔重发订阅消息、最多 `notice.round_limit` 轮（**实时读取配置，改动对未完结任务生效**；调度每小时第 5 分钟扫描，新任务立即发首轮）；确认首次生效。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 任务 active | 创建/窗口变更自动创建（合并进既有 active 任务） | 手动关闭 | 弹窗与重发扫描中 |
| 任务 closed | 超管手动关闭 | — | 停止弹窗与重发 |
| 用户未确认 | 任务创建 | 点「收到」 | 弹窗与重发目标 |
| 用户已确认 | confirm（`confirmed_at` 首次落库） | — | 移出重发名单 |
| 订阅重发完结 | 达 `notice.round_limit`（配置表） | — | **停止订阅消息重发；弹窗通道不设轮次上限**（Q7） |

**异常分支：** 无授权/发送失败记 `send_status=unauthorized/failed` 如实落库（不做假"已送达"）；轮次扫描幂等；confirm 重复调用幂等（首次生效）；**同一学期最多 1 个 active 手动任务，窗口变更通知合并进同一任务**（W18）；弹窗队列上限 `notice.popup_queue_max`（默认 5，超出保留最新 5 条）。

**字段规范：** `notice_record(task_id, user_id, round_no, sent_at, send_status, confirmed_at)`（**主体为用户，覆盖秘书/教师/学生**，W7）；轮次上限与间隔以 `system_config` 为唯一真源、修改对未完结任务立即生效（W8），`notice_task` 两字段为创建时快照；确认请求无输入字段（可带 `subscribe_result`），服务端校验任务归属与幂等；索引 `(task_id, user_id, round_no)`。

**触达机制结论（W5，写入文档避免验收争议）**：微信一次性订阅「一次授权只能发一条」，授权动作挂在「收到」点击 → **未确认者≈未授权者，重发通道对其基本无效**；弹窗通道为主触达（不设轮次上限），订阅消息定位为「已授权用户的额外提醒」，未授权者进线下兜底名单；真催办渠道留 V1.1 评估。

**触达承诺（Q8）**：教师/秘书仅弹窗触达（打开 Web 时拉取），不保证必达；学生=弹窗+订阅消息双通道。

**错误文案：** 发送状态枚举 sent/unauthorized/failed；确认接口成功返回 204。

#### 模块 8：供货商只读、导出与全局规范

**功能描述**：供货商物理隔离只读接口；导出中心（同步/异步 + 一次性 token）；全局异常与错误文案令牌。

**异常分支（全局）**：参数校验失败 400（逐字段 message）；未认证 401（三类细分）；越权 403+审计；窗口冲突 409；状态冲突 409（提示刷新）；下载 token 失效 410；服务错误 5xx（不泄露堆栈）。

**状态机：** 供货商无业务状态；导出任务 queued → running → done/failed/expired（过期清理）。

**字段规范：** 统一响应包络 `{code, message, data}`；分页参数 `page`/`size`（默认 1/20，上限 200）；供货商清单字段白名单=**书名/ISBN/数量/教师姓名/学院**；导出文件流带一次性 token（单次有效，默认 10 分钟，文件保留 24 小时）；**导出异步阈值：预估行数 >5000 走 `export_task` + 轮询**。

**导出清单**：教师征订明细（秘书本院/教材室全院）、**秘书本院签字版（Excel 含签字栏）**、**学生选购汇总（参考用量，仅教材室）**、供货商清单（一学院一 sheet）、通知汇总（含角色）、错误明细。

**错误文案（全局令牌）**：400「请求参数有误」；401「登录已过期，请重新登录」；403「无权执行该操作」；409「数据状态已变更，请刷新后重试」；410「下载链接已失效，请重新导出」；500「服务开小差了，请稍后重试」。每次供货商导出写审计日志（谁/何时/范围）。

#### 模块 9：系统配置与审计（新增）

**功能描述**：系统参数维护 + 全操作审计留痕与查询。

**配置键清单（M1 冻结）**：`notice.round_limit`(5)、`notice.interval_hours`(24)、`notice.popup_queue_max`(5)、`order.quantity.max_default`(999)、`order.correct_window_days`(7)、`export.sync_row_threshold`(5000)、`export.download_token_minutes`(10)、`import.max_file_mb`(10)。

**异常分支**：配置值非法（非数字/越界）→ 400 逐字段提示；配置变更立即影响未完结任务（重发参数）；配置与审计变更均记录操作者。

**状态机**：无显式状态（键值维护）；审计为只读追加。

**字段规范**：`config_key` 唯一且受白名单约束（不在清单内的键拒绝写入）；审计字段 = 操作者/动作/资源/资源 id/详情 JSON/IP/时间；审计不含密码与 token。

**错误文案**：「配置项不存在」「配置值不合法」「时间范围不正确」。

### 五、六大盲区自检清单（服务端适配版）

- [x] **接口结构**：模块分包 + 接口分组树明确（含 `/api/supplier/**` 统一前缀），供货商物理隔离红线可机检（CI ArchUnit）。
- [x] **异常交互**：每个模块均含异常分支说明与全局异常码表（400/401/403/409/410/5xx）。
- [x] **状态机**：账号、会话、学期、窗口、批次、表单、选购、审批、通知、导出十类状态机完整。
- [x] **字段规范**：各模块关键字段 + 校验规则齐备；公共字段、`deleted` 语义与 `reserve1~6` 预留字段规范统一。
- [x] **文案规范**：全局错误文案令牌 + 各模块专属文案。
- [x] **流程图**：登录/首登/导入/审查/通知/导出流程含成功/失败分支与出口。

### 六、安全设计自检清单

**账户安全**
- [x] **首登校验**：初始密码=学号/工号后 6 位，首登前禁止访问业务数据 + 手机号后 4 位/openid 绑定校验后才可改密（W19/R9）。
- [x] **人机验证**：失败 5 次锁 15 分钟（**落库**，重启不清）+ IP/账号双维度限频（V1）；撞库出现再引入验证码。
- [x] **会话安全**：access 15 分钟 + refresh 7 天轮换，登出/停用/改密/角色变更即撤销全部 refresh。
- [x] **频率限制**：登录/导出接口限频；全局网关限流。
- [x] **全局熔断**：Nginx + 应用层限流两级。

**内容安全**
- [x] **UGC 审核**：无公开 UGC；教师表单经超管内容审核后计入汇总（先审后汇总）。

**文件安全**
- [x] **文件类型限制**：仅 .xlsx，服务端魔数校验。
- [x] **文件大小限制**：≤10MB（可配置），超大分批。
- [x] **存储方案**：临时目录，导入后清理；导出文件保留 24 小时后清理。
- [x] **防盗链**：导出一次性授权 token（单次有效 + 10 分钟过期）。

**AI 安全**
- [x] 无 AI 功能，不适用；字段审查/自动开关窗/重发轮次均为确定性规则，白名单化可审计。

**传输安全**
- [x] **HTTPS**：全站强制（moonzj.com 已备案）。
- [x] **敏感数据**：BCrypt；名单不含身份证字段（G1）；JWT 环境变量密钥。
- [x] **CORS**：同域反代，仅小程序跨端（合法域名白名单）。

**基础设施安全**
- [x] **环境变量**：DB/AppSecret/JWT 密钥注入，不硬编码（清单见 SPEC §13）。
- [x] **依赖审计**：定期依赖漏洞扫描。
- [x] **日志脱敏**：不含密码/token；审计仅记操作者与范围。
- [x] **备份**：MySQL 每日全量备份保留 14 天（W22）。

> 账号锁定/停用状态机见模块 1；全局错误文案令牌见模块 8；传输层异常（401/403）由全局异常码表统一处理。

自检通过："PRD 落地版已完成，六大盲区 + 安全设计 + AI 增强 8 大区块均已覆盖。"

---

## 七、问题校验（AI 增强）

- [x] **目标用户是否太泛**：服务对象=5 类角色前端 + 定时任务，边界清晰。
- [x] **MVP 是否过大**：P0 聚焦隔离/窗口/双缓冲/审查/导入导出；审批与通知闭环 P1；分库分表、多实例、短信渠道移入停车场。
- [x] **需求是否单一**：只解决「征订业务统一后端」一件事。
- [x] **验证可行性**：Spring Boot 3 + MySQL 8 + EasyExcel 全为成熟栈；万行级在单机容量内（异步批次）；域名备案已完成。

## 八、输入设计（AI 增强 · 复用于数据输入）

| 输入项 | 来源 | 类型 | 是否必填 | 约束/校验规则 | 示例 |
|--------|------|------|----------|---------------|------|
| Excel 文件 | Web 上传 | 文件 | 是 | .xlsx ≤10MB；行级严格校验；幂等 upsert；**停用比对限本次覆盖范围** | 学生全量表.xlsx |
| 窗口配置 | 超管 | JSON | 是 | start<end；乐观锁版本号 | {"windowStart":"...","windowEnd":"..."} |
| 审核结论 | 超管 | JSON | 是 | action=pass 必填理由为空 / reject 理由 1-200 字必填 | {"action":"reject","reason":"..."} |
| 系统配置 | 超管 | JSON | 是 | 键白名单 + 值域校验 | {"notice.round_limit":5} |
| JWT 凭据 | 前端请求头 | string | 是 | 签名 + 过期 + 角色版本号三重校验 | Authorization: Bearer ... |
| 刷新凭据 | 前端请求体 | string | 刷新时是 | 未撤销 + 未过期 + 用户状态正常 | {"refreshToken":"..."} |

- [x] 输入缺失给出兜底提示（逐字段 message）。
- [x] 输入异常（空/超长/格式错）处理路径定义（400+逐字段错误）。

## 九~十二、AI 输出设计 / AI Workflow / AI 职责拆解 / Badcase 分析

**本服务无 AI 功能，AI 输出设计、AI Workflow、AI 职责拆解不适用。** 全部自动判断（字段审查、窗口自动开关、重发轮次）为确定性规则代码，规则白名单化、可配置、可审计。若以 AI 职责动词描述：本服务不存在由模型执行的抽取/分类/匹配/排序/生成任务——最接近的自动判断（ISBN 匹配、错误行分类汇总、字段审查规则执行）均由确定性代码完成，无需失败兜底到人工以外的降级路径。

**Badcase 分析（≥8 个，系统边界/异常场景）：**

| 场景 | 可能原因 | 风险等级 | 应对策略 | 是否需要人审 |
|------|----------|----------|----------|--------------|
| [场景1-双缓冲切换与在途写冲突] | 切换瞬间有未完成提交 | 高 | 切换单事务+乐观锁；在途请求按旧学期快照完成；切换持全局锁，窗口扫描跳过 | 否 |
| [场景2-万行导入中途失败] | 超时/格式异常/内存 | 中 | 异步批次+failed 可重传+错误行收集不中断 | 是（错误行人工核对） |
| [场景3-窗口自动开关定时任务重复执行] | 任务重叠/重启 | 中 | 幂等设计（状态已变跳过）+进程内锁+仅扫 active 学期 | 否 |
| [场景4-越权改 ID 拉取别院数据] | IDOR 探测 | 高 | 数据隔离切面（多角色并集）+资源归属校验+越权矩阵测试 | 否（测试必测） |
| [场景5-供货商接口探测学生字段] | 接口枚举 | 高 | `/api/supplier/**` 物理隔离无学生字段路径 + CI ArchUnit 机检 + 审计 | 否（测试必测） |
| [场景6-同一教材被并发修改] | 教材库并发编辑 | 低 | 乐观锁+更新时间戳比对 | 否 |
| [场景7-订阅消息 token 失效批量发送失败] | access_token 过期竞态 | 中 | token 集中管理+刷新锁；failed 如实落库下轮重试 | 否 |
| [场景8-重复确认/重复重提风暴] | 前端重试/网络抖动 | 中 | confirm 与提交幂等（唯一约束+首次生效） | 否 |
| [场景9-初始密码被冒用首登] | 学号公开可猜 | 高 | 首登校验（手机号后 4 位/openid）+ 改密前禁访问业务数据 + 审计异常首登 | 是（异常首登人工核查） |
| [场景10-单学院导入误停用其他学院] | 停用比对范围过宽 | 高 | 比对限本次导入覆盖范围 + 结果摘要列出停用数量 | 是（停用名单人工确认） |
| [场景11-补正重提被窗口校验拦死] | 窗口校验未设例外 | 中 | `@WithinWindow` 对驳回表单补正豁免 + 补正窗口可配置 | 否 |

## 十三、验证目标

| 验证项 | 方式 | 通过标准 |
|--------|------|----------|
| RBAC 与隔离 | 越权矩阵自动化用例（5 角色 × 资源 × 操作） | 100% 拒绝 + 审计留痕 |
| 窗口引擎 | 自动开关/延长/提前截止演练 | 状态流转正确、通知自动创建、时区正确 |
| 双缓冲 | 切换演练 + 在线会话并发 + 归属校验 | 原子切换，业务无感，active 归属正确，同刻仅一个 active |
| 通知闭环 | 弹窗确认幂等 + 重发 5 轮 + unauthorized 统计 | notice_record 时间线完整；**5 轮重发在试运行环境验收（本地不可验）** |
| 导入幂等与范围 | 同文件重复上传 + 单学院导入 | 无重复数据；不误停用范围外账号；万行 ≤5 分钟 |
| 性能 | 万行导入 / 并发查询 | 批次进度正常，列表接口 P95 < 500ms |
| 导出安全 | 一次性 token 复用/过期测试 | 单次有效，过期返回 410 |

## 十四、PRD 风险和下一步

**风险**
1. 订阅消息授权率不可控且**一次性订阅机制上限**（未确认者≈未授权者）→ 弹窗为主触达 + unauthorized 如实统计 + 线下兜底（R1/R10）；
2. 初始密码=学号后 6 位的账号劫持风险 → 首登校验 + 改密前禁访问业务数据（R9）；
3. 单机定时任务多实例部署会重复执行 → 当前单实例部署，多实例时换分布式锁/xxl-job；
4. 学校重部署漂移 → 配置外置+初始化脚本+部署手册+备份脚本交付（R6）；
5. 后端单线人力在 M4 任务密度最高 → 签字版导出复用既有导出链路，超期时看板降级为单接口聚合（R13）。

**下一步**
1. M1：工程骨架 + RBAC 五表 + 权限码种子 + JWT(access+refresh) + 首登校验 + 审计切面 + 种子数据；**OpenAPI 契约冻结**（与 Web/小程序并行开发）；
2. M2：窗口引擎 + 双缓冲 + 异步导入中心（含 5 模板与导入顺序）；
3. M3：教师征订两级审查 + 学生选购 + 隔离切面 + 供货商只读模块；
4. M4：通知确认闭环 + 异动审批（含批量）+ 看板统计 + 系统配置 + 审计查询 + 秘书签字版导出；
5. M5：各角色导出 + 越权测试 + 边界回归 + 部署手册/初始化脚本/备份脚本，moonzj.com HTTPS 试运行（含订阅消息 5 轮重发验收）。
