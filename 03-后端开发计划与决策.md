# 教材征订系统 · 后端开发计划与决策（v3 · 复审修订版）

> 更新：2026-09-21 · 决策状态：**三轮拍板完毕**（01 §6 两轮 + 05 §2 复审 16 问 + 06 §4 追问 W1-W24 默认值全部确认），无遗留待定项
> 本版变更：按 **05 §4** 六项 + **06 §5** 二十项清单执行；W1-W24 默认值全部采纳为锁定决策（回执见 §0）
> 姊妹文档：需求决策见 01 号；前端计划见 02 号 v3；评审与追问见 05/06 号；**DDL 全文、接口清单、实现规格见 `textbook-order-server/SPEC.md`**
> 变更基线：本版同时修订 03 §3.2/§4/§5/§6/§7/§8/§10/§11 与新增 §5（双缓冲）、§10（配置与审计）、§14（契约）、§15（运维）

---

## 0. 本版变更摘要（W1-W24 决议回执 · 2026-09-21）

| W | 决议（默认值全部采纳） | 落点 |
|---|----------------------|------|
| W1 | 双缓冲 = 同库同表 + `semester_id` 隔离 + `active_status` 状态位；切换=单事务两条 UPDATE + 乐观锁；归档=仅置状态、数据只读保留 | **新增 §5** |
| W2 | `school_class` 增 `student_count`，数量上限=该班人数；缺失则回退 `system_config` 固定值（999） | §6.1、§8 |
| W3 | 学生选书清单 = 本班任课关系下**教师已提交且超管审核通过**的教材并集；有教师提交即标必修；驳回后标「已下架」不删已选项 | §6.2 |
| W4 | `@WithinWindow` 对「被驳回表单的补正重提」豁免（仅限本人该表单、状态 ∈ {rejected, rejected_auto}）；补正窗口=关窗后 7 天或超管手动关闭 | §6.1 |
| W5 | 承认订阅消息机制上限：重发仅对「已授权且未确认」生效，未授权记 `unauthorized` 进线下兜底名单；弹窗为主触达；真催办留 V1.1 | §7、R10 |
| W6 | 新增 `user_semester_profile` 为按学期归属真源；`sys_user` 两列降为「当前 active 学期」冗余 | §5.2、§8 |
| W7 | `notice_record.student_id` → `user_id`；索引改 `(task_id, user_id, round_no)`；订阅消息仅发 student；汇总导出字段按角色扩展 | §7、§8 |
| W8 | `system_config` 为重发参数唯一真源（对未完结任务立即生效）；`notice_task` 两字段降级为创建时快照 | §7 |
| W9 | `order_form UNIQUE(semester_id, teacher_id, deleted)`、`student_order UNIQUE(semester_id, student_id, deleted)`；`deleted` 改 `BIGINT DEFAULT 0` | §8 |
| W10 | 数据范围 = 各角色**并集**；切换身份只改界面与权限码，**不放宽数据范围** | §3.2 |
| W11 | 新增落库 `window_status` 为唯一真源；`auto_open/auto_close=false` 时到点不改状态；扫描仅 active 学期；时区固定 `Asia/Shanghai` | §4 |
| W12 | M1 以 springdoc-openapi 的 OpenAPI 3 为唯一契约源，`sys_permission` 种子 SQL 随契约冻结；供货商前缀统一 `/api/supplier/**` | §14 |
| W13 | M1 冻结五张模板列清单；导入顺序 = 学院→专业→班级→教师→任课→学生→异动 | §9.1 |
| W14 | 停用比对仅限本次导入文件覆盖范围；导入为权威源；冲突以导入为准并提示「覆盖 N 条异动结果」 | §9.3 |
| W15 | 异动通过后对 active 学期**立即生效**，历史按快照；导出同时给出快照归属列 | §6.3 |
| W16 | 教师异动只校验 工号存在/目标学院存在/前后值不同（无班级）；可变更字段 = `college_id` | §6.3 |
| W17 | 不做独立「圈定征订范围」功能：范围 = 已导入的 `teacher_course` 关系 | §6.1 |
| W18 | 学生选购汇总作「参考用量」导出给教材室（不进供货商清单）；供货商清单**增数量列**；同学期最多 1 个 active 手动通知任务，窗口变更通知合并进同一任务，弹窗队列上限 5 条 | §6.2、§7、§9.4 |
| W19 | 保留初始密码规则但加约束：首登前不可访问业务数据 + 首登校验手机号后 4 位（或绑定 openid）后才允许改密 | §3.3、R9 |
| W20 | 登录锁定落库（`sys_user.lock_until` + `fail_count`），Caffeine 仅做限频加速 | §3.3 |
| W21 | refresh 落库 `sys_user_token(user_id, token_hash, expire_at, revoked, device_id)`，轮转=旧作废发新；access 15 分钟、refresh 7 天 | §3.3 |
| W22 | 量化验收：万行导入 ≤5 分钟且进度可查、列表接口 P95<500ms、越权矩阵 100% 拒绝；M5 增备份/守护/证书续期/归档定义 | §12、§15 |
| W23 | 后端**单线**开发流 + 张敬统筹；总工期 7 → **7.5 周**（M2 +0.5 周）；「签字版导出」从 M3 移到 M4 | §12 |
| W24 | 窗口状态/服务器时间接口 + 窗口变更记录查询 → M2；系统配置页 + 审计查询页 → M4；11 个 skill 迁移到当前工具链技能根 | §12、§13 |

---

## 1. 技术栈（锁定 · 含版本，对齐 02 §1.1 的锁定力度）

| 层 | 选型 | 锁定版本 |
|----|------|----------|
| 语言/框架 | Java 17 (LTS) + Spring Boot | Boot **3.3.x**（M1 落地时锁定补丁号并写入 pom） |
| ORM | MyBatis-Plus（`mybatis-plus-spring-boot3-starter`，含 DataPermissionInterceptor） | **3.5.x** |
| DB | MySQL | **8.0.36+**（utf8mb4 / utf8mb4_0900_ai_ci） |
| Excel | EasyExcel（流式读写） | **3.3.x** |
| 定时任务 | Spring `@Scheduled` + 配置表参数 + 进程内锁（单机） | 随 Boot；多实例部署时换 xxl-job |
| 鉴权 | JWT（jjwt）+ Spring Security 过滤器链 | jjwt **0.12.x** |
| 微信生态 | weixin-java-miniapp（code2session / access_token 集中管理 / 订阅消息） | **4.6.x** |
| 缓存 | 进程内 Caffeine（限频加速/窗口状态读缓存）；Redis 仅在多实例时引入 | **3.1.x** |
| 契约 | springdoc-openapi（OpenAPI 3 为唯一契约源） | **2.6.x** |
| 构建 | Maven **3.9+**（`mvnw` 入库），产物 = 可执行 jar | — |
| 测试 | JUnit 5 + Spring Boot Test + Testcontainers（MySQL） | Testcontainers **1.20.x** |
| 部署 | Nginx(HTTPS+反代) + Spring Boot jar(systemd) + MySQL，单机 | — |

**明确不做**：微服务、消息队列、代码生成器依赖、Redis、多模块拆分。

## 2. 部署拓扑（E4 + Q13 子路径 + 运维交付）

```
开发：本地（前端 Vite proxy /api → 本地后端；后端 local profile）
试运行：阿里云 Ubuntu + moonzj.com —— 根域继续跑已有服务，本系统在子路径
        Nginx: location /textbook/ → SPA 静态目录 + try_files 回退
               location /api       → 反代 Spring Boot（独立 location）
移交学校：同拓扑重新部署（改 env 文件与 Nginx，不改代码）
```

- **接口一律相对路径**（Web 同域 `/api`；小程序 baseUrl 单点配置于 `utils/config.js`）。
- 配置外置：`application-{env}.yml` + 环境变量（DB、微信 AppSecret、JWT 密钥）；**配置项清单见 SPEC §13**。
- HTTPS 必开；微信 access_token 单机内存刷新加锁管理（R5）。
- **交付物**（R6 强化，W22）：部署手册（含 Nginx 配置模板、子路径约定、移交检查单）+ 数据库初始化脚本（含 `sys_permission` 种子）+ 种子数据 SQL（M1）+ **MySQL 每日备份脚本（mysqldump，保留 14 天）** + systemd unit + 证书续期说明。

## 3. RBAC、账号与数据隔离（锁定）

### 3.1 权限五表与权限码清单（W12：清单随契约冻结）

`sys_user` / `sys_role` / `sys_permission` / `sys_user_role` / `sys_role_permission`；权限码格式 `模块:业务:操作`；多角色支持（A4-A）：一人多行 `sys_user_role`，登录返回角色列表 + 切换接口。

**权限码清单（M1 冻结，共 36 条，作为 `sys_permission` 种子 SQL 与 OpenAPI 一并交付）**

| 模块 | 权限码 | 说明 |
|------|--------|------|
| semester | `semester:semester:manage` | 学期新建/编辑 |
| semester | `semester:semester:activate` | 学期激活（双缓冲切换）/归档 |
| semester | `semester:window:manage` | 窗口设置/开启/提前截止/延长 |
| semester | `semester:window:view` | 窗口状态查看（全角色） |
| account | `user:account:manage` | 建号/停用/启用（含供货商） |
| account | `user:account:reset` | 重置密码 |
| org | `org:college:manage` / `org:major:manage` / `org:class:manage` | 组织三表维护 |
| textbook | `textbook:book:manage` / `textbook:book:import` | 教材 CRUD/停用、教材导入 |
| course | `course:course:manage` / `course:teacher:manage` | 课程维护、任课关系 |
| people | `people:student:import` / `people:teacher:import` | 名单导入 |
| order | `order:form:submit` | 教师填报提交/补正 |
| order | `order:form:view:self` / `order:form:view:college` / `order:form:view:all` | 本人/本院/全院查看 |
| order | `order:form:review` | 内容审核（通过/驳回） |
| student | `student:order:submit` / `student:order:view:self` / `student:order:view:all` | 学生选购提交/本人/全院查看 |
| change | `change:request:submit` / `change:request:review` | 异动提交、异动审批（含批量） |
| batch | `import:batch:view` | 导入批次进度与错误明细 |
| export | `export:order:create` / `export:signature:create` / `export:student:create` / `export:notice:create` | 四类导出 |
| notice | `notice:task:manage` / `notice:task:view` | 通知任务创建关闭、进度与失败名单 |
| dashboard | `dashboard:stat:view` | 数据看板 |
| config | `config:config:manage` | 系统配置 |
| audit | `audit:log:view` | 审计日志查询 |
| supplier | `supplier:order:view` / `supplier:order:export` | 供货商清单查看、导出 |

### 3.2 数据隔离切面（W10 修订：多角色并集 + 身份切换不放宽）

```
@CollegeScope（MyBatis-Plus DataPermissionInterceptor 自动改写 SQL）：
  ADMIN      → 不过滤
  SECRETARY  → college_id = 本人所属学院
  TEACHER    → user_id = 本人（教师维度：teacher_id = 本人）
  STUDENT    → user_id = 本人
  多角色     → 各角色范围取【并集】（如 秘书+教师 = college_id=X OR teacher_id=me）
  切换身份   → 只改变界面与权限码呈现，【不放宽数据范围】（防「切到教师身份看全院」越权）
SUPPLIER     → 不进入本切面：独立包 + `/api/supplier/**` 独立前缀 + 禁止 import 学生/教师相关 Mapper
               （机检规则：ArchUnit 或 CI 脚本扫描 supplier 包 import 列表，命中即失败）
@WithinWindow：填报/选购提交接口校验 channel_open && window_status=open；
               例外：被驳回表单的补正重提（W4，见 §6.1）；查看/导出不挂
```

### 3.3 账号规则（锁定 + W19/W20/W21 修订）

- 无注册接口；Excel 全量导入即建号；**初始密码 = 学号/工号后 6 位**（G1），`must_change_password=true`；供货商账号超管手工建。
- **首登流程（W19）**：初始密码登录成功 → 状态=待改密 → **改密前一律拒绝业务接口**（仅 `/api/me*`、改密、首登校验可用）→ 首登校验手机号后 4 位（或绑定 openid）通过后才允许设置新密码 → 状态=正常。
- 密码策略：BCrypt；新密码 8-64 位且含字母和数字（对齐 server PRD 模块 1）；改密全员通用；超管重置（重置为初始密码规则 + `must_change_password=true`）。
- **登录锁定（W20）**：失败计数与锁定时间**落库**（`sys_user.fail_count` / `lock_until`），失败 5 次锁 15 分钟；Caffeine 仅用于 IP/账号维度限频加速，重启不清锁。
- **JWT 会话（W21 / Q6）**：access 15 分钟 + refresh 7 天；refresh 落库 `sys_user_token(user_id, token_hash, expire_at, revoked, device_id)`；轮转 = 旧 refresh 作废 + 发新；登出/停用/改密/角色变更 → **撤销该用户全部 refresh**；access 携带 `role_version`，与库中版本比对，停用/角色变更即时失效。
- 401 语义三类（供前端分流，契约冻结项）：access 过期（可 refresh 重放）/ refresh 失效或角色版本失效（强制登出）/ 账号停用（强制登出）。
- 停用不删除；学期导入与新名单比对停用，**比对范围见 §9.3（W14）**。
- openid：`wx.login` → code2session 静默收集存 `sys_user.openid`，**仅用于订阅消息推送，不作认证**（A6-A）。

## 4. 征订窗口引擎（W11 修订：状态落库）

### 4.1 数据（`semester`）
`window_start`、`window_end`、`channel_open`（总开关）、`auto_open`、`auto_close`、**`window_status`(not_open/open/closed)**、`active_status`(draft/active/archived)、`version`（乐观锁）。

### 4.2 行为状态机
```
not_open ──(到点自动[auto_open=true] / 超管手动开启)──> open
open     ──(到点自动[auto_close=true] / 超管提前截止)──> closed
closed   ──(超管延长, 无限次)──> 更新 window_end；若已 closed 则 channel_open=true + window_status=open
```
- `window_status` 为**唯一真源**（落库，非推导）；`@Scheduled` 与手动操作都写它。
- `@Scheduled` 每分钟扫描：仅扫 **active 学期**（draft/archived 不参与）；`auto_open=false` / `auto_close=false` 时到点**不改状态**（保留手动控制）；幂等（状态已变则跳过）。
- **时区固定 `Asia/Shanghai`**（JVM `-Duser.timezone` + 定时任务 ZoneId 双保险），服务器 UTC 不影响窗口判定。
- 提前截止后可再次延长（channel_open 复位 + 新 window_end）；延长可无限次。
- **延长/提前截止 → 自动创建/合并系统通知任务**（source=system_window_change，见 §7）：内容含本次延长时间与新截止时间、提醒尽快提交；接收范围=秘书+教师+学生全量（G4；触达口径见 §7）。
- 所有窗口变更写 `audit_log`（谁/何时/原值/新值）。
- 并发防护：进程内锁 + `version` 乐观校验，杜绝重复开启/关闭。
- **接口（W24，M2）**：`GET /api/semester/window/status`（含 `serverTime`，供前端倒计时，PRD 功能 2「不信任本地时钟」）、`GET /api/admin/semester/{id}/window/changes`（变更记录，供 02 §6.2 页面展示）。

## 5. 学期数据双缓冲（新增章节 · W1/W6 定稿）

> 03 v2 只有「双缓冲」这个名字，本节补齐机制；这是 M2 的核心交付，也是 M3 的前置。

### 5.1 学期域表清单（谁进缓冲区）
| 分类 | 表 | 说明 |
|------|----|------|
| **学期域**（按学期隔离） | `course`、`teacher_course`、`order_form`、`order_form_item`、`student_order`、`student_order_item`、`notice_task`、`notice_record`、`user_semester_profile`、`change_request` | 均带 `semester_id`；查询一律带当前 active 学期 |
| **跨学期共用** | `college`、`major`、`school_class`、`textbook`、`sys_user`、`sys_role`、`sys_permission`、`system_config` | 不随学期切换；`textbook` 可停用旧书（B5） |

### 5.2 归属的按学期存储（W6，双缓冲能否成立的关键）
- **真源**：`user_semester_profile(user_id, semester_id, college_id, class_id, status)`——学生/教师在某学期的学院班级归属。
- `sys_user.college_id/class_id` 保留为**「当前 active 学期」冗余列**，仅在学期切换时由 profile 同步写入。
- **新学期导入只写 profile（该学期的 draft 行），不触碰 active 学期的归属** → 这是「导入无感」的实现前提（v2 直接改 `sys_user` 会污染 active 学期归属）。
- 数据隔离切面按 profile 归属过滤（秘书看本院 = profile.college_id 匹配）；异动审批写 profile（W15 立即生效），历史表单/选购按提交快照不变。

### 5.3 切换与归档
```
导入(写 draft 学期数据 + profile) → 校验（结构完整性/必填/数量核对） → 人工确认
→ 单事务：UPDATE semester SET active_status='archived' WHERE id=旧active
          UPDATE semester SET active_status='active'   WHERE id=新学期（校验 version 乐观锁）
          + 同步 sys_user 归属冗余列（由 profile 写入）
→ 归档 = 仅置 archived，数据只读保留（可查可导，D2-A）
```
- 同一时刻仅一个 active：数据库层 `UNIQUE KEY uk_active`（生成列 `active_flag = IF(active_status='active',1,NULL)`，`UNIQUE(active_flag)`）保证；应用层乐观锁 + 二次确认提示。
- 切换失败（版本冲突）→ 事务回滚 + 提示「存在更新的学期状态，请刷新」。

### 5.4 并发与在途请求
- 切换瞬间的在途写：已提交请求按**请求进入时的 active 学期快照**完成（写旧学期数据，不串学期）；新请求读新 active。
- 切换期间窗口引擎扫描跳过（持同一把锁），避免「切换中窗口状态错配」。
- 导入期间的查询零影响：draft 学期数据不被 active 查询命中（查询恒带 `semester_id = active`）。

## 6. 两级审查（教师单 + 学生选购 + 异动）

### 6.1 教师征订单（含 W2/W4/W17）
```
submitted
→ ①系统字段审查（自动，客观白名单规则）：
    必填完整 / 数量>0 且 ≤ 上限 / 教材存在且在库 / 课程归属本人 / 课程-班级关联存在 / ISBN 格式(ISBN-10/13)
    ├─ 任一不过 → 自动驳回(rejected_auto)，错误逐字段返回 {field, rule, message}（教师可修复后窗口内无限次重提）
    └─ 全过 → pending_review
→ ②超管内容审核：reviewed(通过) / rejected(驳回，理由必填 1-200 字)
```
- **数量上限来源（W2）**：`school_class.student_count`（导入名单时自动计算或随模板带出）；该班人数缺失或为 0 → 回退 `system_config` 的 `order.quantity.max_default`（默认 999）。
- **关窗后被驳回的补正（G5 + W4）**：`@WithinWindow` 对补正重提**豁免**——仅限「本人该表单」且状态 ∈ {rejected, rejected_auto}；补正窗口 = 关窗后 7 天或超管手动关闭补正入口（先到为准，由 `system_config.order.correct_window_days` 配置）。
- 始终不补正 → 导出中标注「已驳回」（默认不计入汇总）。
- **「禁止全驳回」落地**：① 规则集全为教师可控可修复的客观项，规则文档化进接口文档（无隐藏规则）；② 驳回必填理由。
- **征订范围（W17）**：不做独立功能——范围 = 已导入的 `teacher_course`（课程×班级）关系；「本学期不征订某课」= 不导入该关系。
- 并发：唯一约束 `UNIQUE(semester_id, teacher_id, deleted)` 保证一人一学期一单；提交以覆盖语义裁决。

### 6.2 学生选购（W3/W5/W18）
- **清单生成规则（W3，服务端唯一实现，前端只渲染）**：清单 = 本班全部「课程-班级」关联下、**教师已提交且超管内容审核通过（reviewed）** 的教材并集；有教师提交即标「必修」；清单随教师表单状态实时变化；教师表单后续被驳回时该教材标「已下架」，**不自动删除学生已选项**，提交时服务端剔除并提示。
- 提交校验：窗口内（`window_status=open`）；数量 1 ≤ qty ≤ 9（对齐 PRD 学生字段规范），数量上限同时受班级人数约束；一人一学期一单，重提 = 整单覆盖；记录 `submit_snapshot`（提交时 college/class 归属）。
- 免审（B3）：学生选购为意向统计，不进入两级审查。
- **下游用途（W18）**：学生选购汇总作为**「参考用量」**导出（教材室，`export:student:create`，含 书名/ISBN/班级/数量/合计）；**不进入供货商清单**（供货商禁触学生数据）。

### 6.3 异动两级审批（含 Q10 批量、W15/W16）
```
教师或秘书提交（逐条 / Excel 批量）
→ ①系统字段审查：目标学号/工号存在、目标学院存在、（学生）目标班级存在、变更类型合法、变更前后值不同
→ ②超管内容审核：approved / rejected(理由必填)
→ approved 才落库生效：写 user_semester_profile（active 学期归属）
```
- **批量（Q10）**：一个上传批次 = 一个 change_request 批次（逐行生成、共享 `batch_no`，`payload_json` 逐行）；字段审查逐行执行；超管支持**按批次批量通过/驳回**（理由必填）；前端复用导入批次轮询模式。
- **教师异动（W16）**：`type=teacher` 只校验 工号存在 / 目标学院存在 / 变更前后值不同（不校验班级）；可变更字段 = `college_id`。
- **生效时点（W15）**：通过后对 active 学期**立即生效**（更新 profile 归属）；历史表单/选购记录按提交快照保持归属不变；导出同时给出「快照归属」列以便对账。
- 审批通过落库与审计写同一事务。

## 7. 通知确认闭环（W5/W7/W8/W18 修订）

```
通知来源：① 超管手动创建（同学期最多 1 个 active）  ② 系统自动（窗口变更，合并进同一任务，W18）
notice_task: semester_id, title, content, target_roles(默认 student), round_limit, interval_hours,
             source(manual/system_window_change), status(active/closed), closed_by, closed_at
  ※ round_limit / interval_hours 为创建时【快照】（仅展示与追溯）；执行依据 = system_config（W8）

确认信号（C2/C3）：
  学生/教师打开小程序或 Web → GET /api/notice/unconfirmed（阻塞弹窗数据源）
  → 点「收到」→ POST /api/notice/{taskId}/confirm（body 可带 subscribe_result）→ notice_record.confirmed_at 首次落库
  → 已确认 + 无新任务 → 不再通知

重发（仅未确认，每天 1 轮，最多 5 轮，@Scheduled）：
  扫描 active 任务 → 未确认用户 → 微信订阅消息发送（仅 student 角色）
  → 每次尝试写 notice_record(round_no, sent_at, send_status: sent/unauthorized/failed)
  → 达 round_limit（system_config）→ 停止【订阅消息重发】；【弹窗通道不设轮次上限】（Q7），
     直到用户确认或超管手动关闭任务；/notice/unconfirmed 对已停止重发但未确认的任务仍返回
```
- **触达机制结论（W5，写入文档以免验收争议）**：微信一次性订阅「一次授权只能发一条」，而授权动作挂在「收到」点击 → **未确认者≈未授权者，重发通道对其基本无效**。故：
  - 弹窗通道为**主触达**（零边际成本、拉取式、不设轮次上限）；
  - 订阅消息定位为「已授权用户的额外提醒」，未授权者记 `send_status=unauthorized` 并进入**线下兜底名单**（导出可查）；
  - 真催办（短信/企业微信/学校统一身份通知）留 V1.1 评估，不在 V1 范围。
- **触达承诺（Q8 明示）**：教师/秘书**仅弹窗触达**（打开 Web 时拉取），不保证必达；学生 = 弹窗 + 订阅消息双通道。
- **主体（W7）**：`notice_record.user_id` 覆盖秘书/教师/学生；订阅消息仅对 student 发送。
- **并发与合并（W18）**：同一学期最多 1 个 active 手动任务；窗口变更通知**合并进同一任务**（累加内容，不新建）；前端弹窗队列上限 5 条（按时间倒序保留最新 5 条，全量在「我的」页可查）。
- 汇总导出（Web，教材室）：学号/工号、姓名、角色、学院、班级、渠道、各轮发送时间、发送状态、确认状态、确认时间（C4 扩展字段集）。
- `notice_record` 为历史表，按学期归档（逻辑归档=按 semester_id 查询隔离），量级上万 × 5 轮，索引 `(task_id, user_id, round_no)`。

## 8. 数据库设计（锁定版）

### 8.1 公共字段与逻辑删除（W9）
每表必有：`id BIGINT PK AUTO_INCREMENT`、`created_at`、`updated_at`、`created_by`、`updated_by`、**`deleted BIGINT NOT NULL DEFAULT 0`**（0=未删；删除时写当前时间戳/操作者 id，规避逻辑删除与唯一索引冲突）、预留字段 `reserve1 ~ reserve6 VARCHAR(255) DEFAULT NULL`（E5）。

### 8.2 表清单（含本版新增表）

| 表 | 关键字段 | 本版变更 |
|----|----------|---------|
| `sys_user` | user_no, name, password_hash, college_id, class_id, phone, status, must_change_password, openid, **fail_count, lock_until, role_version** | +锁定落库、+role_version；college_id/class_id 注为「当前 active 学期冗余」 |
| `sys_user_token` | user_id, token_hash, expire_at, revoked, device_id | **新增（W21 refresh）** |
| `sys_role` / `sys_permission` / `sys_user_role` / `sys_role_permission` | 五表 | 权限码清单见 §3.1 |
| `college` / `major` / `school_class` | 父 id、名称、grade、全称；`school_class` **+student_count** | +班级人数（W2） |
| `user_semester_profile` | user_id, semester_id, college_id, class_id, status | **新增（W6 归属真源）** |
| `semester` | name, start/end_date, window_start, window_end, channel_open, auto_open, auto_close, **window_status**, active_status, version | +window_status 落库（W11） |
| `textbook` | isbn(唯一), title, edition, author, press, price, status | 跨学期共用 |
| `course` / `teacher_course` | code/name；teacher_id, course_id, class_id, semester_id | 学期域 |
| `order_form` | semester_id, teacher_id, status(draft/submitted/rejected_auto/rejected/pending_review/reviewed), field_check_result JSON, review_by/at/note | +唯一约束 |
| `order_form_item` | form_id, course_id, class_id, textbook_id, quantity | |
| `student_order` / `student_order_item` | semester_id, student_id；item 含 textbook_id, quantity, submit_snapshot | +唯一约束；快照 |
| `change_request` | type(student/teacher), target_user_id, payload_json, status, field_check_result JSON, applicant_id, reviewer_id, review_at, reason, **batch_no** | +batch_no（Q10） |
| `import_batch` | biz_type, file_name, semester_id, total/ok/error, progress_pct, status, error_detail | |
| `export_task` | biz_type, params_json, row_estimate, file_path, download_token, token_expire_at, status, progress_pct, expires_at | **新增（Q16/W18）** |
| `notice_task` | semester_id, title, content, target_roles, round_limit, interval_hours, source, status, closed_by/at | 语义修订（W8/W18） |
| `notice_record` | task_id, **user_id**, round_no, sent_at, send_status, confirmed_at | student_id→user_id（W7） |
| `system_config` | config_key, config_value, remark, updated_by | **重发参数唯一真源（W8）** |
| `audit_log` | user_id, action, resource, resource_id, detail_json, ip, at | +查询接口（W24） |

### 8.3 唯一约束与索引（W9）
- 唯一：`sys_user(user_no, deleted)`、`textbook(isbn, deleted)`、`order_form(semester_id, teacher_id, deleted)`、`student_order(semester_id, student_id, deleted)`、`user_semester_profile(user_id, semester_id, deleted)`、`semester(active_flag)`（生成列，保证同刻仅一个 active）、`sys_user_token(token_hash)`。
- 常规索引：`order_form(semester_id, teacher_id)`、`student_order(semester_id, student_id)`、`notice_record(task_id, user_id, round_no)`、`change_request(batch_no)`、`import_batch(biz_type, status)`、`export_task(status)`、隔离列 `sys_user(college_id)`、`user_semester_profile(semester_id, college_id)`、`audit_log(user_id, at)`。
- **DDL 全文见 `textbook-order-server/SPEC.md` §3**（01 §3.3 承诺的「详细 DDL 见 03 号文档」由 SPEC 承接）。

## 9. Excel 导入导出（W13/W14/W18 修订）

### 9.1 模板列清单（M1 冻结，共 5 张 + 可增）
| 模板 | 列 |
|------|----|
| 学生全量 | 学号、姓名、学院、专业、班级、手机号 |
| 教师全量 | 工号、姓名、学院、手机号 |
| 教材库 | ISBN、书名、版次、作者、出版社、单价、状态 |
| 课程任课 | 课程代码、课程名、教师工号、班级名称、学期 |
| 异动 | 学号/工号、变更类型、目标学院、目标班级、原因 |
（名单**不含身份证字段**，G1；初始密码取学号/工号后 6 位。）

**导入顺序（W13，外键依赖）**：学院 → 专业 → 班级 → 教师 → 课程任课 → 学生 → 异动。

### 9.2 异步导入
**上万行 → 上传落盘（≤10MB，魔数校验）→ import_batch 落库 → `@Async` + 专用线程池 EasyExcel 流式解析 → progress_pct 轮询**；错误行收集 JSON（不中断解析）可下载；严格模式（不自动创建学院/班级）；幂等 upsert（user_no / ISBN 存在则更新）。

### 9.3 停用比对范围与导入权威性（W14）
- **停用比对仅限本次导入文件覆盖的范围**（按文件内学院 + 角色限定），范围外账号一律不动 → 避免「只导一个学院导致其他学院全部停用」（F4 开发期为单学院规模）。
- **导入是权威源**：导入可覆盖 `class_id` 等归属；异动用于学期内微调；二者冲突时**以导入为准**，并在导入结果摘要中提示「覆盖 N 条异动结果」。
- 导入写 `user_semester_profile`（draft 学期行），不触碰 active 学期归属（§5.2）。

### 9.4 导出（W18/Q4/Q16 修订）
| 导出物 | 角色/权限码 | 说明 |
|--------|------------|------|
| 教师征订明细 | 秘书本院 `export:order:create` / 教材室全院 | 多维度筛选 |
| **秘书本院签字版** | 秘书 `export:signature:create` | Excel 含签字栏（学院盖章/教材室签字/日期三行；版式样张由田老师提供，逾期按占位降级），**排期 M4** |
| **学生选购汇总（参考用量）** | 教材室 `export:student:create` | 书名/ISBN/班级/数量/合计；**不进供货商清单** |
| 供货商清单 | 供货商 `supplier:order:export` | 一学院一 sheet；字段 = 书名/ISBN/**数量**/教师姓名/学院；每次导出写 audit_log |
| 通知汇总 | 教材室 `export:notice:create` | C4 扩展字段集（含角色） |
| 错误明细 | 各导入人 `import:batch:view` | 导入错误行下载 |

- **异步阈值（Q16）**：预估行数 ≤ 5000 → 同步流式下载；> 5000 → `export_task` + 轮询 + **一次性授权下载链接**（`download_token` 单次有效 + `token_expire_at`，默认 10 分钟；文件保留 24 小时）。
- 导出正确性验收在 **M5**（Q3），页面/权限码在 M3 验收。

## 10. 系统配置与审计（新增章节 · W24）

### 10.1 系统配置（`system_config` 键清单，M1 冻结）
| config_key | 默认值 | 用途 |
|-----------|--------|------|
| `notice.round_limit` | 5 | 订阅消息重发轮次上限（唯一真源，W8） |
| `notice.interval_hours` | 24 | 重发间隔小时 |
| `notice.popup_queue_max` | 5 | 弹窗队列上限（W18） |
| `order.quantity.max_default` | 999 | 班级人数缺失时的数量上限回退值（W2） |
| `order.correct_window_days` | 7 | 关窗后补正窗口天数（W4） |
| `export.sync_row_threshold` | 5000 | 导出同步/异步阈值（Q16） |
| `export.download_token_minutes` | 10 | 一次性下载 token 有效期 |
| `import.max_file_mb` | 10 | 上传文件大小上限 |

- 管理入口：**M4 新增「系统配置」页（超管，`config:config:manage`）**，后端提供列表/更新接口，变更写审计。

### 10.2 审计（`audit_log`）
- 留痕范围（E6）：登录、导出、账号操作、窗口变更、学期切换、审批、复核、配置变更、供货商导出。
- **M4 新增「审计日志」查询页（超管，`audit:log:view`）**：按 操作者/动作/资源/时间范围 过滤 + 分页。
- 审计写入与业务操作**同事务**（审批/切换/复核等关键动作）；日志不含密码/token。

## 11. 风险登记（R1-R8 修订 + R9-R13 新增）

| # | 风险 | 缓解 |
|---|------|------|
| R1 | 订阅消息授权率不可控 | 弹窗为主触达（不设轮次上限）+ 「收到」时请求授权 + 首登引导 + 「我的」页授权入口 + `unauthorized` 如实统计曝光未触达名单（**机制上限见 R10**） |
| R2 | ~~域名备案~~ **已消除**：moonzj.com 已 ICP 备案且合规运行（G2） | Web 可直接部署试运行；小程序上架前完成小程序备案 |
| R3 | 小程序为**个人主体**（G3），上架类目受限 | 小程序仅教师/学生使用；先开发/体验版测试；上架按个人主体适配类目；Web 走学校主体 |
| R4 | ~~身份证合规~~ **已消除**：名单不含身份证字段（G1） | 初始密码=学号/工号后 6 位 |
| R5 | access_token 多处刷新互踢 | SDK 单例集中管理 + 刷新锁 |
| R6 | 学校移交重部署漂移 | 配置外置 + 相对路径 + 部署手册 + 初始化脚本 + **备份脚本（W22）** |
| R7 | 并发与幂等：窗口切换竞态 / 万行导入 / 重提覆盖 | 状态变更加锁+乐观校验；导入批次数核对；**唯一约束（W9）+ 覆盖语义** |
| R8 | 关窗瞬时提交 | 服务端 `@WithinWindow` 校验，超时 409，前端友好提示 |
| **R9** | **账号劫持**：初始密码=学号后 6 位，学号为公开信息，冒用者可首登改密夺取账号 | 首登前禁止访问业务数据 + 首登校验手机号后 4 位/openid 绑定后才可改密（W19）；导入后即提示学生尽快首登；异常首登可查审计 |
| **R10** | **订阅消息机制上限**：一次性订阅「一次授权一条」，未确认者≈未授权者 → 重发对目标人群基本无效 | 弹窗为主触达（Q7 不设轮次上限）+ `unauthorized` 名单线下兜底；真催办留 V1.1 评估其他渠道（W5） |
| **R11** | **导入误停用**：全量导入「消失者自动停用」在单学院开发期会误停其他学院 | 停用比对限定在本次导入文件覆盖范围（W14）；导入结果摘要列出停用数量供人工确认 |
| **R12** | **双缓冲在途写**：切换瞬间有未完成提交 / 切换中窗口状态错配 | 在途请求按进入时学期快照完成；切换持全局锁，窗口扫描跳过（§5.4） |
| **R13** | **后端单线人力**：7.5 周单线 + 前端 8.5 周并行，M4 任务密度最高（通知+审批+看板+签字版导出） | M4 的签字版导出复用教师明细导出链路（增量小）；M1 末契约冻结后前端 mock 并行；若 M4 超期，优先保通知链路、看板降级为单接口聚合 |

## 12. 里程碑（后端视角 · **7.5 周** · W23/W24 修订）

| 里程碑 | 周数 | 内容 | 验收标准（量化） |
|--------|------|------|-----------------|
| **M1 骨架** | 1 | 工程初始化（Maven/依赖版本锁定）、RBAC 五表 + **权限码种子 SQL**、登录/JWT(access+refresh)/强制改密/**首登校验**/切换身份、组织三表、审计切面、**种子数据 SQL + 测试账号清单**、**OpenAPI 契约冻结（含 401 三类/字段审查回显/system_config 上限接口）** | 5 类账号登录得正确权限码；种子账号每角色 ≥3 个（正常/停用/首登待改密）可登录；OpenAPI 产出并交付前端；`sys_permission` 种子随契约冻结 |
| **M2 数据基座** | **2**（+0.5） | 学期 + **窗口引擎（自动开关/延长/提前截止 + 自动通知任务 + window_status 落库 + 状态/服务器时间/变更记录接口）** + **双缓冲（学期域表 + user_semester_profile + 原子切换 + 归档）** + 异步 Excel 导入中心（5 模板 + 导入顺序 + 停用范围） | 万行导入 ≤5 分钟且进度正确；窗口自动开关/延长生效；draft→active 原子切换成功且切换后 active 归属正确；停用比对不越界；**前端「学期与窗口引擎」页全流程可走** |
| **M3 业务核心** | 2 | 教师征订单 + 两级审查（含关窗补正豁免）+ **学生选购（清单规则 + 校验 + 快照）** + 隔离切面（多角色并集）+ 供货商只读模块（`/api/supplier/**` 机检） | 字段审查通过项全过、错误项逐字段报错；秘书只见本院；供货商拿不到学生字段（机检通过）；关窗后补正可重提、其余提交 409；学生清单随教师审核状态正确变化 |
| **M4 通知与审批** | 1.5 | 弹窗通知确认接口 + 订阅消息重发实现 + notice_record(user_id) + 汇总导出 + 异动两级审批（逐条+批量）+ **看板统计 API** + **系统配置页接口** + **审计查询接口** + **秘书签字版导出（W23 移入）** | 确认落库且幂等；弹窗队列上限与合并生效；`unauthorized` 如实落库；审批通过才落库（同事务）；看板三/四指标真实数据；配置变更立即影响未完结任务；签字版导出可用 |
| **M5 导出与验收** | 1 | 各角色导出（含学生选购汇总）+ 越权/IDOR 测试 + 边界回归（**S1-S6、S9-S13** + G5 补正流程 + R1-R13）+ **部署手册 + 初始化脚本 + 备份脚本 + systemd unit** + **订阅消息试运行验收** | 全用例通过；列表接口 P95<500ms（1 万行数据）；越权矩阵 100% 拒绝且有审计；moonzj.com HTTPS 试运行成功；**5 轮重发/unauthorized 统计在试运行环境验收（本地不可验）** |

**人力与 owner（Q12/W23）**：后端单线（1 个 AI 开发流）+ 张敬统筹；接口契约 owner = 张敬（M1 末冻结，两线共同遵循）；部署手册/备份脚本 owner = 张敬；种子数据 owner = 后端线（M1 第一周交付）；订阅消息模板申请 = 张敬（**M1 首周**提交，个人主体类目风险提前暴露）；小程序备案/上架 = 张敬（M5 后处理）。

**与前端并行**：前端 8.5 周（02 §9），后端 7.5 周；M1 末冻结契约；**M5 两侧合流联调**。后端 M2/M3 交付先于前端同里程碑验收点，接口供给不阻塞前端。

## 13. 配套 Skill（已安装 + W24 迁移说明）

| 里程碑 | 后端侧 skill |
|--------|-------------|
| M1 | `basic-system-design`（契约评审）、`rest-api-conventions`（包络/分页/错误码）、`spring-security-jwt`（JWT+过滤器链+RBAC+refresh） |
| M2-M3 | `layered-architecture`（分层约束）、`transactional-patterns`（双缓冲切换/导入事务）、`idempotency-patterns`（重提覆盖/导入 upsert/confirm 幂等） |
| M5 | `testing-pyramid`（单测/切片/Testcontainers）、`basic-testing-strategy`（测试计划：越权矩阵 + S1-S6/S9-S13 + R1-R13） |

**迁移说明（W24）**：11 个 skill 当前位于 `E:\Desktop\work\TIAN\.claude\skills\`（04 号文档 §0 的「已安装」陈述属实），但 ZCode 会话的技能根为 `~/.agents/skills` 与 `~/.zcode/skills`，**这些 skill 与 `weixin-devtools-mcp` 在当前工具链下未被加载**。M1 开工前需迁移到当前工具链技能根并同步 MCP 配置；未迁移前本节按「待迁移」对待，不构成里程碑验收项。

## 14. 接口契约与冻结流程（新增章节 · W12）

- **唯一契约源**：springdoc-openapi 生成的 **OpenAPI 3**（`/v3/api-docs`，试运行环境受权限保护或关闭），随代码生成，不手写第二份。
- **契约冻结清单（M1 末）**：① OpenAPI 全量；② `sys_permission` 种子 SQL（权限码清单）；③ 401 三类语义与错误码细分（access 过期 / refresh 失效 / 账号停用）；④ 字段审查错误回显格式 `{field, rule, message}`；⑤ `system_config` 数量上限读取接口；⑥ 统一响应包络 `{code, message, data}` 与分页参数 `page`/`size`（默认 1/20，上限 200）。
- **冻结后变更流程**：契约变更记录（谁/何时/哪个端点/前后差异）+ 前后端双方确认 + 同步更新前端 SPEC；**前端 mock 与页面路径以冻结后的 OpenAPI 为准**（当前 web SPEC §6 的路径为提议值）。
- **接口前缀**：`/api/auth/**`、`/api/me/**`、`/api/admin/**`、`/api/secretary/**`、`/api/teacher/**`、`/api/student/**`、**`/api/supplier/**`**（物理隔离，W12 统一；v2 的 `/supplier-api/**` 作废）；**接口清单见 SPEC §11**。

## 15. 运维与交付（新增章节 · W22）

| 项 | 内容 |
|----|------|
| 进程守护 | systemd unit（`textbook-order-server.service`，`Restart=always`，`-Duser.timezone=Asia/Shanghai`） |
| 备份 | `mysqldump` 每日 02:00 全量 + 保留 14 天；备份脚本与恢复步骤进部署手册 |
| 证书 | Nginx HTTPS 证书续期说明（certbot 或云厂商方案） |
| 日志 | 应用日志按天轮转（logback，保留 30 天）；不含密码/token（PRD 日志脱敏） |
| 归档 | 学期归档 = 置 `active_status=archived`，数据只读保留、可查可导；不做物理迁移（D2-A） |
| 移交检查单 | 环境变量清单、DB 初始化、种子/权限码导入、Nginx 双 location、备份任务、管理员账号交接 |
